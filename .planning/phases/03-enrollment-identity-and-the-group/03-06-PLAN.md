---
phase: 03-enrollment-identity-and-the-group
plan: 06
type: execute
wave: 5
depends_on: ["03-05"]
files_modified:
  - .planning/phases/03-enrollment-identity-and-the-group/03-DEVICE-PASS.md
autonomous: false
requirements: [NDG-02, NDG-07, ENR-13, DEL-02, DEL-03, DSG-05, DSG-06, LNG-06]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The nudge escalates in the copy and never in the frequency. Five outcomes of the deadline arithmetic are exercised by moving the configured deadline locally rather than by waiting: more than seven days, more than one day, exactly one, exactly zero, and past. The middle branch is reachable for a six day window in the whole life of this site, so waiting is not a testing strategy."
    - "The bar shows at most once per page load and is dismissible for the session. No path increases how often it appears as the deadline nears, and no path re-shows it after a dismissal."
    - "Every source gate the phase set is re-run as one sweep at the end, so a regression introduced by a later plan is caught before the phase closes rather than after."
    - "copy.js closes the phase at 156 keys per table at identical key sets, which is the 114 it entered with plus the 42 the UI contract specifies. Parity is verified rather than assumed, which is what the requirement actually asks for."
    - "The guest-count field's zero and overflow branches are exercised against real config values rather than reasoned about: at zero the field is absent from the form entirely and zero extra guests is sent, and above four it renders a native select over the same range."
    - "Requirement checkboxes are derived per ID from what was verified, never from how complete the code looks. Anything that cannot be checked without a real phone stays unchecked until the device sheet carries a result, which is the precedent phase 2 set and STATE.md records."
    - "The researcher's probe row and the tracer's probe row are both removed at the end rather than at the start. The row was the only data in the table, which made it a genuinely useful fixture for verifying the head count arithmetic, the sort and the below-threshold hiding path."
    - "The cleanup is proved by reading the public view back, not by the owner's report, because a blocked read and an empty table look identical from the client."
    - "UI consideration, E8 overflow: with the bar shown at 320x568, 375x667, 390x844 and 430x932, each of the countdown clock, the address value and the door video slot can be scrolled clear of the bar, and the footer's last line is fully visible at maximum scroll. The highest risk item in the phase, because the bar had never rendered on any device before this phase and the shipped reserve was short by up to 27px on a notched iPhone."
      verification: backstop
    - "UI consideration, cross-cutting zero-one-many: a maximum of zero extra guests removes the guest field entirely rather than rendering a one-option control, and sends zero."
    - "UI consideration, cross-cutting zero-one-many: a maximum above four takes the documented overflow branch, a native select over the same range."
    - statement: "UI consideration, cross-cutting long-text: every Danish error string fits on one line at 320px inside the reserved error box. The copy contract caps them at 36 characters and the no-reflow guarantee depends on that cap holding at render, not only in the source."
      verification: backstop
  artifacts:
    - path: ".planning/phases/03-enrollment-identity-and-the-group/03-DEVICE-PASS.md"
      provides: "the filled record of everything that could be established at the desk, and the explicit list of what still cannot"
      contains: "Performed on"
  key_links:
    - from: "the closing sweep"
      to: "app.js enrollmentReady() at :1165"
      via: "asserting the gate is byte-identical to its phase 1 body after five plans touched the file"
      pattern: "enrollmentReady"
    - from: "03-DEVICE-PASS.md"
      to: ".planning/REQUIREMENTS.md"
      via: "an ID stays unchecked until the sheet carries a result for it; the verifier derives the ticks, this plan does not"
      pattern: "NDG-02"
---

<objective>
Close the phase honestly. Run every gate the five preceding plans set, as one sweep, so a regression
one plan introduced into another's work is caught before the phase closes. Exercise the two things
that can only be checked by moving config rather than by reading code: the four-branch nudge ladder
and the guest-count field's zero and overflow branches. Fill in everything on the device sheet that
a desk can establish, and state plainly what still cannot. Then have the owner remove the two test
rows, which were kept deliberately until now because they were the only data in the table and
therefore the only fixture the head count arithmetic had.

Purpose: phase 2 ended by deriving requirement checkboxes per ID from what was actually verified
rather than from how complete the code looked, and it left three IDs unchecked pending a device pass.
That was the right call and STATE.md records it. This plan does the same thing rather than rounding
up.

Output: a re-run of every phase gate, a recorded result for all five nudge branches, a filled desk
section on `03-DEVICE-PASS.md`, and a clean `enrollments` table.
</objective>

## Probe ledger

Phase-wide: 59 probe-surfaced edges equal **42 authored into `must_haves.truths`** plus **17
surfaced as flagged planner assumptions**. This plan authors **0** of the 42, because every edge
belongs to a requirement another plan implements, and flags the **last 1** of the 17.

Per plan, so the totals are checkable rather than asserted: edges authored 16 / 6 / 3 / 15 / 2 / 0;
flagged assumptions 7 / 4 / 1 / 4 / 0 / 1.

`03-UI-SPEC.md` `## UI Considerations` is lifted row by row across the phase. Its header tallies 54
cells; its tables carry **62 rows**, because the header counts only the nine described surfaces'
distinct element-by-category cells and excludes the five cross-cutting rows and E9's second `empty`
row. **All 62 are lifted**, which is the superset of either reading. Per plan: 32 / 6 / 0 / 13 / 7 /
**4**. This plan lifts E8's `overflow` row, which is the phase's highest risk item and belongs here
because this is where the device pass that can answer it is run, plus the three cross-cutting rows
that are config-branch and rendering checks rather than build work.

The two rows `03-UI-SPEC.md` marks **unresolved**, both on the withdraw confirmation, are carried and
resolved in plan 05, which builds that surface. They are not dropped and they are not silently
counted as satisfied here.

## Flagged planner assumption (unclassified edge, this plan's 1, and the phase's 17th)

| Requirement | Assumption the plan proceeds on |
|---|---|
| NDG-07 | That "escalates as the date approaches, and it is the copy that escalates, not the frequency" is fully satisfied by the four-branch copy ladder plus a bar that shows at most once per load and stays dismissed for the session. There is no frequency dimension to test, because none exists in the code: the assumption is that the absence of a frequency mechanism is the requirement being met rather than a gap. Stated so a verifier reads it as an assumption. |

## Artifacts this phase produces

Full phase registry. **Own** marks the plan that creates the symbol. This plan creates no source
symbol; it verifies every row below and fills the record sheet.

| Symbol | Kind | Own |
|---|---|---|
| `store` extension, `identity.get/save/clear`, `newGuestId()` | app.js | 01 |
| `sbConfigured()` / `sbRequest(...)` / `submitEnrollment(...)` / `amendEnrollment(...)` | app.js | 01 |
| `withdrawEnrollment(ident)` | app.js | 05 |
| `renderEnrollment()` / `refreshEnrollmentState()` | app.js | 01 |
| `buildField` / `buildForm` / `buildRecord` / `buildSuccessPanel` / `buildReturnPanel` | app.js | 01 |
| `buildWithdrawnPanel()` | app.js | 05 |
| `setFormState`, the validators, `showFieldError`, `wireField`, `validateAll`, `wireEnrollment` | app.js | 01 |
| `measureNudge()` | app.js | 02 |
| `markGroupJoined()` / `whatsappButton(...)` / `renderWhatsApp()` / `renderSocialProof()` | app.js | 04 |
| The form component system, `.panel`, `.facts--record`, `.sweep`, `.form-alert` | CSS | 01 |
| `.facts--proof` | CSS | 04 |
| `.subtle-action` | CSS | 05 |
| `--grid-record` (R1), `@keyframes sweep-x` (R2), the `[hidden]` base rule | CSS | 01 |
| `--nudge-h`, the toast offset, `scroll-padding-bottom` | CSS | 02 |
| `#enrol-form` and the nine field, hint and error ids | element ids, created in JS | 01 |
| `#wa` `#wa-body` `#enrol-proof` | element ids, static markup | 04 |
| `data-state` on the form, `data-invalid` / `data-errkey` / `data-touched` | attributes | 01 |
| `data-panel` values `success`, `return`, `withdrawn` | attribute values | 01 / 05 |
| `data-mode="edit"` on the form | attribute | 05 |
| `withdrawn` column, the re-created `attendees` view, `public.amend_enrollment(...)` | schema | 03 |
| `config.js` comment corrections | config | 03 |
| **`03-DEVICE-PASS.md` filled** | record sheet | **06** |
| **`COVERAGE.md`** | API coverage record, written at planning time | planner |

**Copy keys.** 114 on entry to the phase, **156 on exit**. Per plan: 01 adds 25, 02 adds 1, 04 adds
2, 05 adds 14. This plan adds none and asserts the total.

<execution_context>
@D:/04. Hoobies & Passions/14. Random Websites/01. Master's Party/.claude/gsd-core/workflows/execute-plan.md
@D:/04. Hoobies & Passions/14. Random Websites/01. Master's Party/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-enrollment-identity-and-the-group/03-CONTEXT.md
@.planning/phases/03-enrollment-identity-and-the-group/03-UI-SPEC.md
@.planning/phases/03-enrollment-identity-and-the-group/03-DEVICE-PASS.md
@.planning/phases/03-enrollment-identity-and-the-group/COVERAGE.md
@.planning/phases/03-enrollment-identity-and-the-group/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run every gate the phase set, exercise the branches only config can reach, and fill the desk half of the device sheet</name>
  <files>.planning/phases/03-enrollment-identity-and-the-group/03-DEVICE-PASS.md</files>
  <read_first>
    - `03-DEVICE-PASS.md` in full, which already carries the seven tables this task fills in part. Table G is entirely this task's work; Tables A to F are the phone's and stay empty here except where a desk browser can legitimately answer a row
    - `03-RESEARCH.md` §Nudge Bar Audit in full, especially its verification tip explaining which branch is reachable for how long and why moving the deadline is the correct method
    - `03-UI-SPEC.md` `## Executor Checklist` in full: it is the list of gates this sweep re-runs, across markup, styles, app and copy
    - `03-UI-SPEC.md` `## UI Considerations` → the cross-cutting rows on the guest-count maximum at zero and above four, and the row on Danish error strings
    - `03-UI-SPEC.md` `## The Form Component System` → the 36 character error cap and why the reserved box depends on it
    - `app.js` lines 1109 to 1160 (the four-branch ladder and the deadline arithmetic it keys off), 1089 to 1105 (the hero deadline line), 1183 (the session flag)
    - `config.js` the enrollment block, whose deadline and guest maximum are the two values this task moves
    - the five plan summaries for this phase, which record what each plan actually shipped rather than what it intended to
    - `03-CONTEXT.md` D-27, D-29, D-33, D-34, and `.planning/STATE.md` on how phase 2 derived its requirement checkboxes
  </read_first>
  <action>
    Run the whole phase sweep as one pass, from the repository root, and record every result in the
    plan summary. This is not a re-statement of earlier gates for form: five plans edited the same
    four files, and the point of running them together at the end is to catch a regression one plan
    introduced into another's work.

    Then exercise the four-branch nudge ladder, which cannot be checked by reading. Temporarily set
    the configured deadline to produce each of the five outcomes in turn: more than seven days out,
    more than one day out, exactly one day, exactly zero days, and a date already past. Reload for
    each and record which copy key rendered and whether the bar showed at all. The middle branch is
    reachable for a six day window in the whole life of this site, so waiting for it is not a testing
    strategy. Confirm across all five that the bar appears at most once per page load and that nothing
    anywhere increases how often it appears: the escalation is in the copy and only in the copy.
    Restore the configured deadline afterwards and verify it is back to its committed value.

    Then exercise the two guest-count branches the same way. Set the maximum to zero: the guest field
    must be absent from the form entirely rather than rendering a one-option control, and a
    registration must still send zero extra guests. Set it to five: the field must render as a native
    select over the whole range rather than as six segments, which would not fit at 320px. Restore the
    committed value and verify it.

    Then measure the Danish error strings as rendered rather than as authored. The source cap is
    verified by the copy gate, but the guarantee the reserved box depends on is that each one occupies
    one line at 320px, so check them in a browser at that width in Danish and record the result.

    Then fill in `03-DEVICE-PASS.md`. Table G is entirely answerable at the desk and this task
    completes it. In Tables A to F, fill only the rows a desktop browser can legitimately answer, and
    leave every row that needs real hardware empty rather than guessing. Where a row is answered at
    the desk rather than on a phone, say so in its note cell, because a desk result for a row about a
    notched iPhone is not the same evidence and a later reader must be able to tell them apart.

    Record the requirement position per ID in the plan summary, derived from what was verified rather
    than from how complete the code looks. Do not edit `.planning/REQUIREMENTS.md` in this plan: the
    verifier derives the ticks from a verification report, which is the discipline phase 2 established
    and STATE.md records. State plainly which IDs are satisfied, which are pending the device pass,
    and which are pending the owner supplying the group link, and note that the phase's headline
    sentence cannot be demonstrated end to end until that link exists even though every code path for
    it is built and testable against a temporary value.
  </action>
  <verify>
    <automated>node --check app.js &amp;&amp; node -e "global.window={};require('./copy.js');var C=window.PARTY_COPY;var en=Object.keys(C.en).sort().join('|');['it','da'].forEach(function(l){if(Object.keys(C[l]).sort().join('|')!==en){console.error('key set mismatch in '+l);process.exit(1)}});var n=Object.keys(C.en).length;if(n!==156){console.error('expected 156 keys per table, got '+n);process.exit(1)}var bad=[];['en','it','da'].forEach(function(l){Object.keys(C[l]).forEach(function(k){if(k.indexOf('enrol.err.')===0&&C[l][k].length>36)bad.push(l+' '+k)})});if(bad.length){console.error('error strings over the cap: '+bad.join(', '));process.exit(1)}console.log('copy OK 156x3, error cap OK')" &amp;&amp; node -e "global.window={};require('./config.js');var C=window.PARTY_CONFIG;var e=C.enrollment||{};if(e.maxGuestsPerPerson!==2){console.error('maxGuestsPerPerson was left at a test value: '+e.maxGuestsPerPerson);process.exit(1)}if(e.deadline!=='2026-09-26T23:59:00+02:00'){console.error('the deadline was left at a test value: '+e.deadline);process.exit(1)}if(e.showCountFrom!==8){console.error('showCountFrom was left at a test value: '+e.showCountFrom);process.exit(1)}console.log('config restored to committed values')" &amp;&amp; test $(grep -c 'enrollmentReady' app.js) -eq 2 &amp;&amp; node -e "var s=require('fs').readFileSync('app.js','utf8');var a=s.indexOf('function enrollmentReady()');var b=s.indexOf('}',s.indexOf('return configured',a));var body=s.slice(a,b+1);if(body.indexOf('CFG.photos')<0||body.indexOf('enrol-form')<0||body.indexOf('supabaseAnonKey')<0){console.error('enrollmentReady was altered; it is a hard constraint');process.exit(1)}console.log('enrollmentReady untouched after five plans')" &amp;&amp; test $(grep -cE '\.innerHTML[[:space:]]*=' app.js) -eq 0 &amp;&amp; test $(grep -c 'insertAdjacentHTML' app.js) -eq 0 &amp;&amp; test $(grep -c '=>' app.js) -eq 0 &amp;&amp; test $(grep -cE '^[[:space:]]*(const|let)[[:space:]]+[A-Za-z_$]' app.js) -eq 0 &amp;&amp; test $(grep -c 'console.log' app.js) -eq 0 &amp;&amp; test $(grep -c 'console.error' app.js) -eq 0 &amp;&amp; test $(grep -c 'Authorization' app.js) -eq 0 &amp;&amp; test $(grep -c "'PATCH'" app.js) -eq 0 &amp;&amp; test $(grep -c "'DELETE'" app.js) -eq 0 &amp;&amp; test $(grep -c 'Math.random' app.js) -eq 0 &amp;&amp; test $(grep -c 'return=representation' app.js) -eq 0 &amp;&amp; test $(grep -c 'window.confirm' app.js) -eq 0 &amp;&amp; test $(grep -c 'showCountFrom' app.js) -eq 1 &amp;&amp; test $(grep -c "'wa_joined', '1'" app.js) -eq 1 &amp;&amp; test $(grep -c ':has(' styles.css) -eq 0 &amp;&amp; test $(grep -c 'map-sweep' styles.css) -eq 0 &amp;&amp; grep -qF 'sweep-x' styles.css &amp;&amp; grep -qF '--grid-record' styles.css &amp;&amp; grep -qF '--nudge-h' styles.css &amp;&amp; grep -qF 'scroll-padding-bottom' styles.css &amp;&amp; grep -qF '[hidden]' styles.css &amp;&amp; test $(grep -c '@media (prefers-reduced-motion' styles.css) -eq 2 &amp;&amp; test $(grep -cE '\.field__(input|textarea|select)[^{]*\{[^}]*font-size:[[:space:]]*(1[0-5]|[0-9])(\.[0-9]+)?px' styles.css) -eq 0 &amp;&amp; test $(grep -c 'tabindex="-1"' index.html) -eq 2 &amp;&amp; test $(grep -c 'aria-label="Dismiss"' index.html) -eq 0 &amp;&amp; grep -qF 'id="wa"' index.html &amp;&amp; grep -qF 'id="enrol-proof"' index.html &amp;&amp; grep -qF 'add column if not exists withdrawn' supabase/schema.sql &amp;&amp; test $(grep -c 'for select' supabase/schema.sql) -eq 0 &amp;&amp; test $(grep -c 'for delete' supabase/schema.sql) -eq 0 &amp;&amp; test $(grep -c 'returns setof' supabase/schema.sql) -eq 0 &amp;&amp; node -e "var fs=require('fs');['index.html','styles.css','app.js','config.js','copy.js','supabase/schema.sql'].forEach(function(f){if(/[\u2014\u2013]/.test(fs.readFileSync(f,'utf8'))){console.error('em or en dash found in '+f);process.exit(1)}});console.log('dash scan clean across six files')" &amp;&amp; node -e "var s=require('fs').readFileSync('.planning/phases/03-enrollment-identity-and-the-group/03-DEVICE-PASS.md','utf8');var g=s.lastIndexOf('## Table G');if(g<0){console.error('Table G is missing from the device sheet');process.exit(1)}var rows=s.slice(g).split('\n').filter(function(l){return l.indexOf('|')===0&&l.indexOf('---')<0&&l.indexOf('Expected')<0});var empty=rows.filter(function(l){return /\|\s*\|\s*$/.test(l)});if(empty.length){console.error('Table G still has '+empty.length+' unanswered rows; the four branch ladder is a desk check and must be completed here');process.exit(1)}console.log('Table G complete')" &amp;&amp; echo PHASE_SWEEP_PASS</automated>
    <human-check>
      <test>At 320px in Danish, with the bar showing, read every error string by triggering each one in turn. Then set the guest maximum to zero and to five and read the form at 320px for each. Then, if a phone is available, work through Tables A to F of 03-DEVICE-PASS.md, starting with Table A.</test>
      <expected>Every Danish error occupies exactly one line inside the reserved box, and the submit button does not move when one appears. At a maximum of zero the guest row is gone entirely rather than showing a single segment. At five the field is a native select rather than six segments squeezed across the width. Table A's first four rows are the ones to trust least until a real notched iPhone has answered them.</expected>
      <why_human>The 36 character cap is verified in the source by the automated gate, but the guarantee it exists to protect is a rendered one-line fit at a specific width in a specific font, and this project has no rendering harness. Tables A to F are the device pass itself and cannot be established at a desk at all.</why_human>
    </human-check>
  </verify>
  <done>Every phase gate passes as one sweep, all five nudge branches and both guest-count branches have a recorded result, the config is back to its committed values, and the device sheet carries everything a desk can honestly answer.</done>
  <acceptance_criteria>
    - `copy.js` holds exactly **156** keys per table at identical key sets, and every error string is within the 36 character cap in all three languages
    - **the config is restored:** the guest maximum is 2, the deadline is its committed value and the count threshold is 8. This is asserted because this task deliberately edits those values and a test value left behind would ship to a live public site
    - `enrollmentReady()` is byte-identical to its phase 1 body after five plans edited the same file, and `grep -c 'enrollmentReady' app.js` returns exactly 2. This is the phase's one untouchable function and it is checked last, not assumed
    - the full negative gate set returns 0: markup assignment, adjacent-markup insertion, arrow functions, block-scoped declarations, console calls, the bearer header, the update verb, the delete verb, the pseudo-random source, the read-back preference, the native confirm dialog
    - single-source gates hold: `grep -c 'showCountFrom' app.js` returns 1 and `grep -c "'wa_joined', '1'" app.js` returns 1
    - `styles.css` carries every token the phase introduced, has exactly two reduced-motion blocks, no relational pseudo-class, no orphaned keyframe name, and no text control below 16px
    - `index.html` still carries exactly two occurrences of the gated attribute and no hardcoded English dismiss label
    - `supabase/schema.sql` still carries no read policy, no delete policy and no set-returning function
    - zero em dashes and zero en dashes across all six source files, schema included
    - **Table G of `03-DEVICE-PASS.md` has no unanswered rows**, asserted by parsing the table rather than by claiming it. All five deadline outcomes and the dismissal behaviour are desk-checkable and are therefore not allowed to be deferred to the phone
    - the plan summary states a position per requirement ID, separating satisfied from pending-device from pending-owner-input, and does not edit `.planning/REQUIREMENTS.md`
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] The owner removes the two test rows, and the public view proves it</name>
  <action>
    Halt and hand off. Claude cannot remove these rows: there is no delete policy on the table and
    there deliberately never will be, so an anonymous delete answers 204 and deletes nothing, and the
    service_role key was disabled by the owner after it was exposed. Present the instructions below
    verbatim, wait, then run the acceptance probe and read its output rather than accepting a report
    that the statement was run.
  </action>
  <instructions>
    See `<how-to-verify>` below. In short: open the Supabase SQL editor for project
    `aplaxdplwnnlezffatal`, run the single delete statement `STATE.md` has recorded since phase 2,
    then run the probe.
  </instructions>
  <verification>
    `GET {photos.supabaseUrl}/rest/v1/attendees?select=first_name,extra_guests` with only an `apikey`
    header returns an array containing **no entry whose first name is `ZZTEST`**. Read through the
    public view and never through the table: a read of the table returns an empty array whether it
    holds a thousand rows or none, so an empty result there would prove nothing at all.
  </verification>
  <what-built>
    Two rows named `ZZTEST DeleteMe` exist in `public.enrollments`: one the researcher inserted on
    2026-08-14 while probing whether the `attendees` view actually exposes anything, and one the
    tracer in plan 03-01 inserted as its live end-to-end proof. Both were kept deliberately until
    now. Together they were the only data in the table, which made them the only fixture the head
    count arithmetic, the alphabetical sort and the below-threshold hiding path had to work against,
    and deleting them at the start of the phase would have removed the ability to tell a broken
    fetch from an empty table.

    They now need to go, because they quietly inflate the confirmed count the site puts on the page.
  </what-built>
  <how-to-verify>
    This is a human-only step for the same reason the migration was. The site has no delete policy
    and never will: anonymous delete is refused, and it is refused by answering `204 No Content` and
    deleting nothing, so nothing Claude can run from the browser or the command line removes a row.
    The service_role key was disabled by the owner after it was exposed.

    1. Open the Supabase dashboard, project `aplaxdplwnnlezffatal`, then SQL Editor, then New query.
    2. Run exactly this, which is the statement `STATE.md` has recorded since phase 2:

       ```
       delete from public.enrollments where name = 'ZZTEST DeleteMe';
       ```

       It removes both rows, because they share a name and differ only in their guest id.
    3. Then run the acceptance probe below from the repository root and paste its output back here.

    ```
    U=$(node -e "global.window={};require('./config.js');process.stdout.write(window.PARTY_CONFIG.photos.supabaseUrl)")
    K=$(node -e "global.window={};require('./config.js');var p=window.PARTY_CONFIG.photos;process.stdout.write(p.supabaseKey||p.supabaseAnonKey)")
    curl -sS "$U/rest/v1/attendees?select=first_name,extra_guests" -H "apikey: $K"
    ```

    **Done looks like:** `[]`, or an array containing only real registrations and no entry whose
    first name is `ZZTEST`.

    **Not done looks like:** an array containing one or more `ZZTEST` entries.

    Note why the probe reads the public view rather than the table. A read of the raw table returns
    an empty array whether it holds a thousand rows or none, because there is no read policy, so an
    empty result from the table would prove nothing at all. The view is the only path allowed to see
    rows, which makes it the only path that can distinguish a clean table from a blocked read. This
    is the same discipline every write in this phase was verified with.

    If any real guest has registered by the time this runs, their first name will appear in the
    output. That is correct and expected, and it is not a reason to delete anything else.
  </how-to-verify>
  <resume-signal>Paste the probe output. Type "clean" once no ZZTEST entry remains, or describe what came back instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test data to the live head count | Probe rows in the table are counted by the public view exactly like real registrations. |
| a test config value to a public deployment | This plan deliberately edits the deadline and the guest maximum on a site that deploys from the default branch. |
| a desk result to a device claim | A row answered in a desktop browser is weaker evidence than the same row answered on a phone. |
| a verified state to a requirement checkbox | Ticking an ID is an assertion about what was proved, not about what was written. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-34 | Tampering | A test config value left committed and deployed | high | mitigate | This task edits the deadline and the guest maximum in a repository that deploys from the default branch on push, so a forgotten test value ships to real guests. Gated explicitly: the sweep asserts all three enrollment values equal their committed originals, and the gate fails on any other value rather than on a missing one. |
| T-03-35 | Repudiation | A requirement ticked because the code looks complete | high | mitigate | Positions are derived per ID from what was verified, the plan does not edit the requirements file at all, and anything needing hardware stays unchecked until the device sheet carries a result. Phase 2 set this precedent, it caught real problems, and STATE.md records it. |
| T-03-36 | Repudiation | A desk result recorded as though it were a device result | medium | mitigate | Rows answered at the desk are marked as such in their note cell, so a later reader can tell the two kinds of evidence apart rather than inheriting a false confidence. |
| T-03-37 | Information disclosure | Test rows inflating the head count shown to guests | low | mitigate | Removed in the closing sweep and proved through the public view, which is the only path that can distinguish a clean table from a blocked read. Kept until now deliberately, because they were the only fixture the count arithmetic had. |
| T-03-38 | Denial of service | The nudge bar covering the address on a device nobody tested | high | mitigate | Not closable at a desk, and this plan does not pretend otherwise. It is the first table on the device sheet, it is the phase's highest risk item, and the phase does not reach a passed state while it is unanswered. Recorded as abstaining to human review rather than as satisfied. |
| T-03-39 | Tampering | A regression one plan introduced into another plan's work | medium | mitigate | Five plans edited the same four files. Every gate the phase set is re-run as one sweep at the end, including the single-source gates and the untouchable-function check, so a later edit that quietly undid an earlier guarantee is caught before the phase closes. |
| T-03-SC | Tampering | Dependency supply chain | low | accept | No package manager, no lockfile, no build step, nothing installed by this plan or anywhere in this phase. `COVERAGE.md` records the whole external surface: one PostgREST endpoint and one outbound link. |
</threat_model>

<verification>
Run from the repository root:

1. `node --check app.js` exits 0 and the ES5 gates return 0.
2. `copy.js` parity harness reports **156** keys across three identical key sets, and every error
   string is within the 36 character cap in all three languages.
3. The three enrollment config values equal their committed originals.
4. `enrollmentReady()` is byte-identical to its phase 1 body and appears exactly twice.
5. The full negative gate set returns 0 across `app.js`, `styles.css`, `index.html` and the schema.
6. Single-source gates hold: one threshold read, one joined-flag write.
7. Zero em dashes and zero en dashes across all six source files.
8. Table G of `03-DEVICE-PASS.md` has no unanswered rows, asserted by parsing the table.
9. **The cleanup check, which only task 2 can satisfy:** the public view returns no entry whose
   first name is `ZZTEST`. Read through the view and never through the table, because a read of the
   table returns an empty array whether it holds a thousand rows or none.

Tables A to F of `03-DEVICE-PASS.md` are the phase's real remaining verification and they abstain to
human review. This project ships no build step and therefore no device harness, by locked constraint,
and adding one to satisfy a gate would break it. Until those tables carry results, the phase is
`human_needed` rather than `passed`, and the requirement IDs that depend on them stay unchecked.
</verification>

<success_criteria>
- Every gate the phase set passes as one sweep, so no plan quietly undid another's guarantee.
- All five deadline outcomes and both guest-count branches have a recorded result rather than an
  argument from the source.
- The config is back to its committed values, verified rather than remembered.
- The device sheet carries everything a desk can honestly answer, and says plainly what it cannot.
- The table holds no test data, proved through the only path allowed to see rows.
- The requirement ledger states a position per ID and rounds nothing up.
</success_criteria>

<output>
Create `.planning/phases/03-enrollment-identity-and-the-group/03-06-SUMMARY.md` when done.
</output>
