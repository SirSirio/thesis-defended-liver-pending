---
phase: 03-enrollment-identity-and-the-group
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - supabase/schema.sql
  - config.js
autonomous: false
requirements: [ENR-06, ENR-11, ENR-13, CFG-01, CFG-03]
user_setup:
  - service: supabase
    why: "This project has no Supabase CLI, no migrations directory and no service_role key. The schema is applied by the owner pasting supabase/schema.sql into the SQL editor. Until they do, edit and withdraw cannot work at all."
    dashboard_config:
      - task: "Open the SQL editor, paste the whole of supabase/schema.sql, and run it. The file is idempotent and safe to run more than once."
        location: "Supabase Dashboard, project aplaxdplwnnlezffatal, SQL Editor, New query"
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The withdrawn column is added with an idempotent alter, never by re-declaring the table, because a create-if-not-exists will not add a column to a live table and the file's promise that it is safe to run more than once must survive."
    - "The attendees view is re-created with the same three columns, in the same order, with the same types, and only a filter added. That is the one kind of change a replace accepts; a column dropped, renamed, reordered or retyped would need the view dropped, which would take the anon grant with it."
    - "The grant on the attendees view stays immediately after the view definition, where it already is, so a replace preserves it and a future drop cannot silently orphan it."
    - "The amend function returns integer. It never returns a row, a record or a set of enrollments. A definer function that returned rows would hand every guest's note to anyone who can guess a uuid, which leaks strictly more than the read policy this phase exists to avoid."
    - "The function pins an empty search path, so every relation inside its body is schema-qualified. This is Supabase's stated requirement for a definer function and the difference between a scoped primitive and a hole."
    - "Execute is revoked from public before it is granted to anon, and the argument signature is repeated verbatim in both statements, because a function is identified by its argument types and a mismatch silently targets nothing."
    - "No SELECT policy is added to public.enrollments by this plan or anywhere in this phase. The one-way door D-02 holds shut stays shut and the note stays structurally unreadable by the publishable key."
    - "No DELETE policy is added. Withdrawal is a flag and the row stays, because knowing who dropped out is more useful than not knowing."
    - "The view keeps running with its owner's privileges. A comment records that switching it to run with the caller's would make social proof go permanently and silently empty, because the underlying table has no read policy."
    - "The closing owner block excludes withdrawn rows from the head count and adds a second query naming who dropped out, so the number the owner buys food against is the number the site shows."
    - "The migration is proved by a live probe that can tell done from not done, never by the owner's report. Before the re-run the function answers 404 with code PGRST202; after it, the same call answers 200 with a body of 0 for a guest_id that does not exist."
    - "config.js gains no key, loses no key and moves no key. The credentials stay under the photos object even though enrollment now uses them, because moving them breaks the gate this phase is forbidden to touch and pre-breaks phase 4. Only the comments change."
    - "Edge ENR-11 adjacency: the head count query and the who-dropped-out query are adjacent in the closing block and read the same table, so neither can be found without seeing the other."
    - "Edge ENR-11 empty: with zero rows the head count query returns 0 rather than null, because the sum is wrapped in a coalesce."
    - "Edge ENR-11 ordering: the dropped-out query orders by the update timestamp descending, so the most recent withdrawal reads first."
  artifacts:
    - path: "supabase/schema.sql"
      provides: "section 7, the withdrawn column and the filtered view; section 8, the amend function with its revoke and grant; and the rewritten closing owner block"
      contains: "amend_enrollment"
    - path: "config.js"
      provides: "corrected owner-facing comments, no key changes"
      contains: "enrollment"
  key_links:
    - from: "supabase/schema.sql section 8"
      to: "app.js amendEnrollment() from plan 01"
      via: "POST /rest/v1/rpc/amend_enrollment, arguments as JSON keys, integer row count back"
      pattern: "amend_enrollment"
    - from: "supabase/schema.sql section 7 withdrawn column"
      to: "public.attendees"
      via: "the view filters withdrawn rows out server side, so a client cannot forget to"
      pattern: "withdrawn"
---

<objective>
Add the two things the database is missing, then stop and get them applied, honestly.

`03-RESEARCH.md` proved against the live project that an update aimed straight at `enrollments`
matches zero rows and answers 204, because Postgres requires read rights for an update whose filter
reads a column and this schema deliberately grants none. Built as originally specified, the edit and
withdraw controls would have reported success and done nothing, on every device, forever, with no
client-side signal that anything was wrong. This plan adds the `withdrawn` column and a narrowly
scoped `security definer` function that closes that gap without opening the door D-02 exists to hold
shut, then blocks on the owner running the file, because there is no CLI in this project that can.

Purpose: this is the phase's one one-way door and its one owner action. Both are surfaced rather
than buried. Enrollment itself has no dependency on any of it and keeps working throughout (D-36).

Output: `supabase/schema.sql` sections 7 and 8, a rewritten owner-facing closing block, corrected
config comments, and a live probe that can tell a migration that ran from one that did not.
</objective>

## Probe ledger

Phase-wide: 59 probe-surfaced edges equal 42 authored into `must_haves.truths` plus 17 surfaced as
flagged planner assumptions. This plan authors **3** of the 42 (ENR-11: adjacency, empty, ordering)
and flags **1** of the 17.

`03-UI-SPEC.md` `## UI Considerations` covers rendered surfaces; this plan renders nothing, so it
lifts **0** of the 62 rows. The rows that depend on this migration having run, E4's `error` row and
both of E5's unresolved rows, are lifted by plan 05, which builds the controls that call it.

## Flagged planner assumption (unclassified edge, this plan's 1)

| Requirement | Assumption the plan proceeds on |
|---|---|
| ENR-06 | That "edit or withdraw" is satisfied by an amendment the database confirms with a row count, and that a guest never needs to see the amended row read back. They cannot: no read policy exists and none is added. The row count is the whole confirmation, and the receipt they read afterwards is rendered from their own device. |

## Artifacts this phase produces

Full phase registry. **Own** marks the plan that creates the symbol. Nothing here is a new file.

| Symbol | Kind | Own |
|---|---|---|
| `store` extension, `identity`, `newGuestId()` | app.js | 01 |
| `sbConfigured()` / `sbRequest(...)` / `submitEnrollment(...)` / `amendEnrollment(...)` | app.js | 01 |
| `withdrawEnrollment(ident)` | app.js | 05 |
| `renderEnrollment()` / `refreshEnrollmentState()` and the panel builders | app.js | 01 / 05 |
| `setFormState`, the validators, `showFieldError`, `wireField`, `validateAll`, `wireEnrollment` | app.js | 01 |
| `measureNudge()` | app.js | 02 |
| `markGroupJoined()` / `whatsappButton(...)` / `renderWhatsApp()` / `renderSocialProof()` | app.js | 04 |
| The form component system, `.panel`, `.facts--record`, `.sweep`, `.form-alert` | CSS | 01 |
| `.facts--proof` | CSS | 04 |
| `.subtle-action` | CSS | 05 |
| `--grid-record` (R1), `@keyframes sweep-x` (R2), the `[hidden]` base rule | CSS | 01 |
| `--nudge-h`, the toast offset, `scroll-padding-bottom` | CSS | 02 |
| `#enrol-form` and the nine field, hint and error ids | element ids, created in JS | 01 |
| `#wa` `#wa-body` `#enrol-proof` | element ids, static markup | 04 |
| **`withdrawn boolean not null default false` on `public.enrollments`** | schema column | **03** |
| **`public.attendees` re-created with the withdrawn filter** | schema view | **03** |
| **`public.amend_enrollment(uuid, text, smallint, text, text, boolean) returns integer`** | schema function, definer, empty search path | **03** |
| **`revoke all ... from public` then `grant execute ... to anon` on that signature** | schema grants | **03** |
| **The rewritten closing block: head count excluding withdrawn, plus a who-dropped-out query** | schema comment | **03** |
| **`config.js` comment corrections, no key added, moved or renamed** | config | **03** |

**Copy keys.** 114 on entry to the phase, 156 on exit. This plan adds **none**: it renders nothing a
guest reads. Per plan: 01 adds 25 (139), 02 adds 1 (140), 04 adds 2 (142), 05 adds 14 (156).

<execution_context>
@D:/04. Hoobies & Passions/14. Random Websites/01. Master's Party/.claude/gsd-core/workflows/execute-plan.md
@D:/04. Hoobies & Passions/14. Random Websites/01. Master's Party/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-enrollment-identity-and-the-group/03-CONTEXT.md
@.planning/phases/03-enrollment-identity-and-the-group/03-RESEARCH.md
@.planning/phases/03-enrollment-identity-and-the-group/03-PATTERNS.md
@.planning/phases/03-enrollment-identity-and-the-group/COVERAGE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: The one-way door. How ENR-06 gets satisfied, and what stays shut</name>
  <decision>
    Adopt a `security definer` function called through the RPC endpoint as the mechanism for editing
    and withdrawing a registration, which requires the owner to re-run `supabase/schema.sql` once in
    the Supabase SQL editor.
  </decision>
  <context>
    This is a genuine deviation from a locked decision and it is surfaced rather than absorbed.
    D-06 specified an update aimed straight at the table. The research executed that exact request
    against the live project and it returned 204 with a row count of zero: the value did not change,
    and nothing in the response told the client so. This is not a Supabase quirk. Postgres requires
    read rights for an update whose filter reads a column, and D-02 deliberately grants none. Built
    as written, the edit and withdraw buttons would have reported success and done nothing.

    Three decisions in `03-CONTEXT.md` are rated one-way and all three are settled by this single
    choice, which is why there is one gate here rather than three. D-02 is the absence of a read
    policy: adding one later republishes every guest's free-text note to anyone holding the
    publishable key, which is committed in a public repository, and undoing it does not un-publish
    anything already read. D-04 is the withdrawn column. D-35 is the function. D-04 and D-35 are the
    same owner action, one paste into one SQL editor, so they cost one interruption rather than two.

    None of these decisions was confirmed by the owner in conversation: `03-CONTEXT.md` was gathered
    in auto mode and says so plainly. That is why this gate exists rather than being skipped as a
    door already walked through.

    Nothing here blocks a guest from registering. The insert works today and is untouched by every
    option below.
  </context>
  <options>
    <option id="option-a">
      <name>Add a permissive read policy on public.enrollments</name>
      <pros>One line. Makes the plain update work, and also unlocks upsert and both read-back preferences on insert, five capabilities at once.</pros>
      <cons>Republishes every guest's free-text note to anyone holding the publishable key. This is precisely what the schema comment says the model exists to prevent. Genuinely one-way: removing the policy later does not un-publish anything already read.</cons>
    </option>
    <option id="option-b">
      <name>A read policy scoped to a request header, plus column grants</name>
      <pros>Honours the spirit of the privacy model. No function to write.</pros>
      <cons>Two mechanisms to misconfigure instead of none, and a later blanket grant anywhere in the file silently re-opens the notes. The failure is quiet and the blast radius is every guest.</cons>
    </option>
    <option id="option-c">
      <name>A security definer function called through the RPC endpoint (recommended)</name>
      <pros>Adds no read policy of any kind, so the raw table stays exactly as unreadable as it is today. The function body is the entire attack surface: it touches only the row whose guest_id was passed in, never returns row contents, and returns an integer row count, which is what makes an amendment provable under the rule that a status code is not proof. Rides along with the withdrawn column the phase already needs, so it costs no additional owner action. About twenty lines of SQL.</pros>
      <cons>Introduces a deliberate privilege escalation primitive. Any later change to its signature or return type is a security-relevant change. Edit and withdraw do not work at all until the owner re-runs the file, and degrade to an honest pending line until they do.</cons>
    </option>
    <option id="option-d">
      <name>Drop edit and withdraw from the phase</name>
      <pros>No schema change, no owner action, no new primitive.</pros>
      <cons>ENR-06 is a stated requirement. This unchecks it rather than satisfying it, and leaves a guest who cannot come with no way to say so except messaging the host directly.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a, option-b, option-c or option-d</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Sections 7 and 8, the owner's two queries, and the config comments that are now wrong</name>
  <reversibility rating="one-way">The column and the function ship in a file the owner runs by hand against the live database, and every later phase reads the amended shape. The larger door, adding a read policy, is the one this design exists to keep shut.</reversibility>
  <files>supabase/schema.sql, config.js</files>
  <read_first>
    - `03-RESEARCH.md` §Database Contract in full: the quoted live table and view definitions, the paragraph on why the view works and what a change to its invoker setting would silently destroy, the rules a replace imposes on the column list, the recommended addition, the three details that are easy to get wrong, and the owner handoff block
    - `03-RESEARCH.md` §THE BLOCKER, subsection The Fix, and §Wire Contract W3 and W6, for the exact response shapes the client from plan 01 already codes against
    - `03-RESEARCH.md` §Security Domain, in particular the row on an over-broad definer function and the paragraph headed One non-obvious call
    - `03-RESEARCH.md` §Gaps in the Existing Code, G4, for the three factually wrong config comments
    - `03-PATTERNS.md` section 14 (the schema file's banner idiom, quoted from section 5 and the closing block) and section 13 (`config.js`'s owner-facing comment style, quoted from the enrollment block)
    - `supabase/schema.sql` in full. It is 199 lines and the whole file is the contract this task extends
    - `config.js` lines 123 to 142 (the enrollment block, the voice to match), 156 to 158 (the group block), and 176 to 199 (the photos and credentials block, whose comments are the wrong ones)
    - `03-CONTEXT.md` D-02, D-04, D-05, D-30, D-35, D-36, and `## Claude's Discretion` for the two items this task exercises
  </read_first>
  <action>
    Append section 7 to `supabase/schema.sql` following the file's numbered banner idiom exactly: an
    equals rule, the numbered title, a dash rule, plain English written for the owner rather than for
    a programmer, then the SQL. Do not renumber the existing sections.

    Section 7 adds the withdrawn column with an idempotent alter that adds it only if it is not
    already present, defaulting to false and not null. Use an alter rather than editing the table
    declaration: a create-if-not-exists will not add a column to a table that already exists, and the
    file's own promise at line 7 that it is safe to run more than once has to survive this change.

    Section 7 then re-creates the attendees view with the same three columns, in the same order, with
    the same types, adding only a filter that excludes withdrawn rows. That is the one kind of change
    a replace accepts. Keep the anon grant on the line immediately after the view, where it already
    is: a replace preserves grants, a drop would not, and moving the grant away from the view is how
    that fact gets forgotten. Add one comment line recording that this view runs with its owner's
    privileges and that switching it to run with the caller's would make social proof go permanently
    and silently empty, because the underlying table has no read policy. That is the whole reason the
    view works at all, and it is a one-word change away from being destroyed by someone being tidy.

    The owner-facing prose in section 7 should say, in the file's voice, that a guest who can no
    longer come marks themselves withdrawn, that there is no delete and there deliberately never will
    be, that withdrawn rows stop counting toward the head count on the site, and that they stay in the
    table because knowing who dropped out is more useful than not knowing.

    Append section 8, the amend function. Its comment is doing real work and should explain, without
    jargon, why this is a function rather than an update sent from the browser: Postgres will not let
    anyone change a row they cannot read, because working out which rows to change means reading them
    first; nobody can read this table, on purpose; so an update sent from the site matches nothing and
    reports success. This function runs as its owner instead, so it can find the one row it was asked
    for, and it still cannot hand anything back, so the notes stay exactly as private as they were.
    Note that the same trade already described in section 3 applies unchanged: anyone who knows a
    guest_id can amend that registration, and guest ids are unguessable and never appear on the page.

    The function takes the guest_id plus five nullable parameters, coalescing each against the
    existing value so an omitted argument means leave it alone. The note parameter is the one
    exception: null means leave it alone, and an empty string means clear it, which is what lets a
    guest remove a note they no longer want. It returns integer, taking the row count from the
    diagnostics, so 1 means the registration was found and changed and 0 means no row carries that
    guest_id.

    Four details that are individually load-bearing. The function is declared `security definer`. It
    pins an empty search path, which forces every relation in the body to be schema-qualified, and the
    body must be written that way. Execute is revoked from public before it is granted to anon,
    because Postgres grants execute on new functions to public by default and a definer function left
    at that default is a genuine hole. The argument signature is repeated verbatim in both the revoke
    and the grant, because a function is identified by its argument types and a mismatch silently
    targets nothing at all.

    Add one comment line above the function stating that its return type is the security boundary and
    that any change to its signature or return type is a security-relevant change. A definer function
    handing back rows instead of an integer would give every guest's note to anyone who can guess a
    uuid, which leaks strictly more than the read policy this whole design exists to avoid.

    Do not add a read policy. Do not add a delete policy. Do not touch the existing policies, the
    timestamp trigger or the storage section.

    Rewrite the closing owner block, which currently counts withdrawn rows and is therefore now wrong.
    Replace it with: where the guest list lives, that the note column is where dietary requirements
    and messages land and that nothing on the website can read it, a head count query that sums
    plus-ones through a coalesce and excludes withdrawn rows, and adjacent to it a second query
    listing who dropped out, ordered by the update timestamp descending. The two queries stay adjacent
    so neither can be found without seeing the other. The existing trigger already maintains that
    timestamp on every update, so the second query needs nothing new.

    Then correct the three factually wrong comments in `config.js`, and only the comments. The quiz
    block says it is built in phase 4; the roadmap says phase 5. The photos block says it is built in
    phase 3; the roadmap says phase 4. The credentials block says that until both values are filled in
    enrollment and photos show a waiting message, but both have been filled in since 2026-08-13, so
    that sentence should describe what the waiting message is for rather than describe the present
    tense wrongly. Keep the voice exactly: an all-caps banner in a block comment with dash rules above
    and below the title, second person, each key explained by what it buys the owner rather than by
    its type, backtick-quoted key names in prose, short aligned comments on individual values, zero
    jargon.

    Add no config key, remove none and move none. The credentials stay under the photos object even
    though enrollment now uses them too: moving them would break the gate this phase is forbidden to
    touch and would pre-break phase 4. Note the coupling in the comment instead, which is the honest
    fix and costs nothing.
  </action>
  <verify>
    <automated>node -e "global.window={};require('./config.js');var C=window.PARTY_CONFIG;var e=C.enrollment||{};if(e.deadline===undefined||e.maxGuestsPerPerson===undefined||e.showCountFrom===undefined||e.showAttendeeList===undefined){console.error('an enrollment key was moved or removed');process.exit(1)}if(!('inviteUrl' in (C.whatsapp||{}))){console.error('the group invite key was moved or removed');process.exit(1)}var p=C.photos||{};if(!p.supabaseUrl||!(p.supabaseKey||p.supabaseAnonKey)){console.error('the credentials moved out of the photos object, which breaks the gate this phase must not touch');process.exit(1)}console.log('config keys unchanged')" &amp;&amp; grep -qF 'add column if not exists withdrawn' supabase/schema.sql &amp;&amp; grep -qF 'create or replace view public.attendees' supabase/schema.sql &amp;&amp; grep -qF 'where withdrawn = false' supabase/schema.sql &amp;&amp; grep -qF 'grant select on public.attendees to anon' supabase/schema.sql &amp;&amp; grep -qF 'create or replace function public.amend_enrollment' supabase/schema.sql &amp;&amp; grep -qF 'security definer' supabase/schema.sql &amp;&amp; grep -qF "set search_path = ''" supabase/schema.sql &amp;&amp; grep -qF 'returns integer' supabase/schema.sql &amp;&amp; grep -qF 'get diagnostics' supabase/schema.sql &amp;&amp; grep -qF 'revoke all on function public.amend_enrollment(uuid, text, smallint, text, text, boolean) from public' supabase/schema.sql &amp;&amp; grep -qF 'grant execute on function public.amend_enrollment(uuid, text, smallint, text, text, boolean) to anon' supabase/schema.sql &amp;&amp; test $(grep -c 'returns setof' supabase/schema.sql) -eq 0 &amp;&amp; test $(grep -c 'for select' supabase/schema.sql) -eq 0 &amp;&amp; test $(grep -c 'for delete' supabase/schema.sql) -eq 0 &amp;&amp; test $(grep -c 'security_invoker' supabase/schema.sql) -eq 0 &amp;&amp; node -e "var s=require('fs').readFileSync('supabase/schema.sql','utf8');var v=s.lastIndexOf('create or replace view public.attendees');var g=s.indexOf('grant select on public.attendees to anon',v);var f=s.indexOf('create or replace function',v);if(g<0){console.error('the anon grant is missing after the view');process.exit(1)}if(f>=0&amp;&amp;g>f){console.error('the anon grant must sit immediately after the view, before section 8');process.exit(1)}var d=s.lastIndexOf('DONE');if(s.indexOf('withdrawn = false',d)<0||s.indexOf('withdrawn = true',d)<0){console.error('the closing block needs both the head count excluding withdrawn rows and the who-dropped-out query');process.exit(1)}if(s.indexOf('coalesce(sum(extra_guests), 0)',d)<0){console.error('the head count must coalesce the sum so zero rows returns 0, not null');process.exit(1)}console.log('schema shape OK')" &amp;&amp; node -e "var fs=require('fs');['supabase/schema.sql','config.js'].forEach(function(f){if(/[\u2014\u2013]/.test(fs.readFileSync(f,'utf8'))){console.error('em or en dash found in '+f);process.exit(1)}});console.log('dash scan clean')" &amp;&amp; echo SCHEMA_WRITTEN_GATE_PASS</automated>
  </verify>
  <done>The schema file carries the column, the filtered view, the scoped function and a closing block whose head count matches what the site shows, and it is still safe to run more than once.</done>
  <acceptance_criteria>
    - `config.js` loads under Node with every enrollment key, the group invite key and both credential keys in their existing positions. Nothing is moved, because `enrollmentReady()` reads the credentials from the photos object and this phase is forbidden to change that function
    - `supabase/schema.sql` contains an idempotent column add, a replaced view carrying the withdrawn filter, and the anon grant positioned after the view and before section 8, asserted by index comparison rather than by presence
    - the function is declared definer, pins an empty search path, returns integer and reads its row count from the diagnostics
    - the revoke on public precedes the grant to anon and both repeat the six-argument signature verbatim
    - `grep -c 'returns setof' supabase/schema.sql` returns 0. The return type is the security boundary: rows coming back would leak every note to anyone who can guess a uuid
    - `grep -c 'for select' supabase/schema.sql` returns 0 and `grep -c 'for delete' supabase/schema.sql` returns 0. No read policy and no delete policy exists anywhere in the file, before or after this change
    - `grep -c 'security_invoker' supabase/schema.sql` returns 0. The view must keep running with its owner's privileges or social proof goes silently empty
    - the closing block carries both queries, adjacent, with the sum coalesced so zero rows returns 0 rather than null
    - zero em dashes and zero en dashes in both files
    - **This task does not claim the migration is applied.** The file is written; task 3 gets it run
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] The owner re-runs the schema file, and a probe proves it</name>
  <action>
    Halt and hand off. Claude cannot apply DDL to this project: there is no Supabase CLI in the
    repository, no migrations directory, and the service_role key was disabled by the owner after it
    was exposed. Present the instructions below verbatim, wait, then run the acceptance probe and
    read its output rather than accepting a report that the file was run.
  </action>
  <instructions>
    See `<how-to-verify>` below. In short: open the Supabase SQL editor for project
    `aplaxdplwnnlezffatal`, paste the whole of `supabase/schema.sql`, run it, then run the probe.
  </instructions>
  <verification>
    `POST {photos.supabaseUrl}/rest/v1/rpc/amend_enrollment` with only an `apikey` header and the
    body `{"p_guest_id":"00000000-0000-4000-8000-000000000000"}` answers **HTTP 200 with a body of
    `0`**. Before the re-run the same call answers HTTP 404 with code `PGRST202`. That difference is
    the entire acceptance test, and it is read from the wire rather than from the owner's report.
  </verification>
  <what-built>
    `supabase/schema.sql` now carries section 7 (the `withdrawn` column and the filtered `attendees`
    view) and section 8 (the `amend_enrollment` function with its revoke and grant), plus a corrected
    closing block with the two queries the owner actually uses. The file is idempotent and safe to run
    more than once. None of it is in the database yet.
  </what-built>
  <how-to-verify>
    This is a genuine human-only step and it is the only one in the phase. There is no Supabase CLI in
    this project, no migrations directory, and the service_role key was disabled by the owner after it
    was exposed. Nothing Claude can run applies DDL to this project.

    1. Open the Supabase dashboard, project `aplaxdplwnnlezffatal`, then SQL Editor, then New query.
    2. Open `supabase/schema.sql` from the repository root, select the whole file, and paste it in.
       The whole file, not only the new sections: it is written to be safe to run more than once and
       running it whole is what keeps the file and the database honest with each other.
    3. Run it. Expect success with no rows returned.
    4. Then run the acceptance probe below from the repository root and paste its output back here.

    ```
    U=$(node -e "global.window={};require('./config.js');process.stdout.write(window.PARTY_CONFIG.photos.supabaseUrl)")
    K=$(node -e "global.window={};require('./config.js');var p=window.PARTY_CONFIG.photos;process.stdout.write(p.supabaseKey||p.supabaseAnonKey)")
    curl -sS -w '\nHTTP %{http_code}\n' -X POST "$U/rest/v1/rpc/amend_enrollment" \
      -H "apikey: $K" -H "Content-Type: application/json" \
      -d '{"p_guest_id":"00000000-0000-4000-8000-000000000000"}'
    ```

    **Done looks like:** `0` on the first line and `HTTP 200` on the second. The function exists, it
    is callable by the anonymous role, and it correctly reports that it touched no row, because that
    guest_id belongs to nobody.

    **Not done looks like:** a JSON body containing `PGRST202` and `HTTP 404`. That is the schema
    cache saying the function does not exist, which means the file has not been run.

    Note carefully why the acceptance criterion is this probe and not your word for it, and not a
    status code on its own. On this project a blocked read answers with an empty array and a blocked
    delete answers 204, so both look exactly like success. A 200 that carries the integer `0` is the
    only shape that distinguishes a working function from an absent one, and it is the same signal the
    site's own edit and withdraw controls key off.
  </how-to-verify>
  <resume-signal>Paste the probe output. Type "applied" once it reads 0 and HTTP 200, or describe what came back instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous role to the amend function | Anyone holding the publishable key, which is public by design, can call this function. Its body is the entire attack surface. |
| the function's own privileges to the table | The function runs as its owner, so row level rules do not apply inside it. |
| the attendees view to the public | The view is the one deliberate hole in the wall and runs with its owner's privileges. |
| the schema file to the live database | Nothing verifies the two are in sync. The file is a script that produces the database, not a mirror of it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-14 | Elevation of privilege | `public.amend_enrollment` running as its owner | high | mitigate | Bounded by its body and its return type. It touches only the row matching the guest_id it was handed, returns integer and never row contents, pins an empty search path so every relation is schema-qualified, and has execute revoked from public before it is granted to anon. Gates assert the return type, the search path, the revoke-before-grant order and the verbatim signature. |
| T-03-15 | Information disclosure | A future change widening the function's return type | high | mitigate | A comment above the function names the return type as the security boundary, and a gate asserts no set-returning form exists in the file. A definer function returning rows would hand every note to anyone who can guess a uuid, which is worse than the read policy this design rejects. |
| T-03-16 | Information disclosure | Adding a read policy on `public.enrollments` | high | mitigate | None is added, and a gate asserts none exists anywhere in the file. This is the phase's one-way door and it is gated by a blocking decision checkpoint before the change is written. |
| T-03-17 | Information disclosure | Switching the attendees view to run with the caller's privileges | medium | mitigate | Gate asserts the invoker setting appears nowhere in the file, and a comment records that the change would make social proof go permanently and silently empty rather than error. Supabase's linter flags owner-privileged views, and that warning is expected here rather than a defect. |
| T-03-18 | Tampering | Amending someone else's registration by guessing their guest_id | medium | accept | Already the accepted trade recorded in the shipped schema at section 3, unchanged by this plan. 122 bits from a CSPRNG, and the id never appears on the page, in a URL or in a shareable link. It would not be the right trade for anything that mattered, and the file says so. |
| T-03-19 | Repudiation | The owner reporting the migration ran when it did not | high | mitigate | Acceptance is a live probe distinguishing 200 with a body of `0` from 404 with `PGRST202`, not the owner's report. Until it passes, the client degrades to an honest pending line and a guest's registration is untouched. |
| T-03-20 | Denial of service | Repeated calls to the definer function from the public key | low | accept | Same honest scope as D-30. The function is idempotent, bounded to one row, and returns no data. A static site with a public key cannot rate limit; the owner's recourse is the dashboard. Stated rather than claimed as mitigated. |
| T-03-SC | Tampering | Dependency supply chain | low | accept | No package manager, no lockfile, no build step, nothing installed. This plan adds SQL to a file and edits comments in another. |
</threat_model>

<verification>
Run from the repository root:

1. `config.js` loads under Node with every key in its existing position.
2. The idempotent column add, the replaced view with its filter, and the anon grant positioned
   between the view and section 8 are all present, asserted by index comparison.
3. The function is definer, pins an empty search path, returns integer and uses the row-count
   diagnostic; the revoke precedes the grant and both carry the verbatim six-argument signature.
4. `grep -c 'returns setof'`, `grep -c 'for select'`, `grep -c 'for delete'` and
   `grep -c 'security_invoker'` on `supabase/schema.sql` all return 0.
5. The closing block carries both owner queries, adjacent, with the sum coalesced.
6. Zero em dashes and zero en dashes in both files.
7. **The applied check, which only task 3 can satisfy:** the amend function called with a guest_id
   belonging to nobody answers HTTP 200 with a body of `0`. Before the owner runs the file the same
   call answers HTTP 404 with code `PGRST202`, and that difference is the entire acceptance test.

Nothing downstream of this plan may assume the migration ran. Plan 05 carries a precondition
asserting the probe result, and the client degrades to an honest pending line until it passes.
</verification>

<success_criteria>
- The schema file carries the column and the function, and is still safe to run more than once.
- No read policy and no delete policy exists anywhere in the file, before or after this change.
- The function cannot hand back a row, and the file says why that matters in a comment.
- The owner's head count query returns the same number the site shows.
- The migration is proved applied by a probe that can tell done from not done.
</success_criteria>

<output>
Create `.planning/phases/03-enrollment-identity-and-the-group/03-03-SUMMARY.md` when done.
</output>
