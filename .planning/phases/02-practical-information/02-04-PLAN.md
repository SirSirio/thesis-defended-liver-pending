---
phase: 02-practical-information
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - config.js
  - copy.js
  - app.js
  - styles.css
  - index.html
autonomous: true
requirements: [ACC-01, ACC-02, ACC-03, ACC-05, DEL-02, DEL-03]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The video element carries playsinline AND muted together. Either alone fails inline playback on iOS Safari, so both are asserted as a pair, plus controls and preload=metadata, and autoplay is absent (D-13)."
    - "The jump to #access is instant under prefers-reduced-motion and smooth otherwise. The target receives focus so the jump works for keyboard and screen reader users, not only visually (D-20)."
    - "With door.posterSrc null the poster attribute is omitted entirely rather than set to an empty string. An empty poster makes Safari request the page itself as an image and is forbidden (D-14)."
    - "Two distinct paths reach the same panel: videoSrc null renders the pending panel without constructing a video element, and a configured videoSrc that returns 404 swaps the errored player for the same panel via its error handler. No browser error text and no developer message reaches a guest."
    - "The access section is built complete against door.videoSrc null. The moment the owner drops a file into assets and sets one config line, the player appears with no code change (D-11)."
    - "UI consideration, empty video: the pending panel sits inside .video-slot at the configured aspect ratio, so there is zero layout shift on the day the file arrives."
    - "UI consideration, loading video: preload metadata is native, a poster shows when configured, and the surface token sits behind the element when not, so an unpainted frame or a letterbox bar is institutional grey and never black (D-14)."
    - "UI consideration, error video source: the error event replaces the slot contents with the pending panel, reusing the phase 1 copy because to a guest a missing file and an unmade file are the same event and the same correct message."
    - "A clip filmed upright on a phone does not letterbox into black pillars: door.aspect drives an inline custom property on the slot and a portrait orientation attribute caps the slot width."
    - "The back to map affordance is an inline link rather than a third large button, because it is a within-page jump and not an action, and a second prominent map affordance would duplicate the location section's own buttons."
    - "Native video controls are not restyled. A hand-rolled player is pure failure surface on iOS Safari and the controls attribute is required by ACC-01."
    - "The video element survives a language switch: it is held in a module-scope reference and re-appended rather than reconstructed, so playback is not interrupted and metadata is not refetched."
    - statement: "Verified by hand on real iOS Safari and real Android Chrome before the phase closes: inline playback with no fullscreen takeover, clipboard copy, directions links opening the native app. No automated harness exists in this project, so this abstains to human review rather than passing silently (D-23)."
      verification: backstop
    - statement: "At a 320px viewport the page does not scroll horizontally with the real address present. The address value and the notes value cell carry overflow-wrap break-word so a long Danish street name wraps instead of pushing the layout."
      verification: backstop
  artifacts:
    - path: "config.js"
      provides: "door.aspect defaulting to 16/9, documented for someone who filmed it upright"
      contains: "aspect"
    - path: "copy.js"
      provides: "access.video.heading and access.back in en, it and da"
      contains: "access.back"
    - path: "app.js"
      provides: "buildVideo and the back link, appended in the fixed access order"
      contains: "function buildVideo("
    - path: "styles.css"
      provides: ".video-slot with its aspect custom property and portrait cap, .inline-link--back"
      contains: ".video-slot"
    - path: "index.html"
      provides: "focus targets on the two jumped-to sections"
      contains: "tabindex=\"-1\""
  key_links:
    - from: "app.js renderAccess()"
      to: "app.js buildVideo()"
      via: "third block in the fixed order, after written directions and practical notes"
      pattern: "buildVideo\\(\\)"
    - from: "app.js buildVideo()"
      to: "config.js door.videoSrc, door.posterSrc and door.aspect"
      via: "defensive read of CFG.door, with poster omitted entirely when null"
      pattern: "CFG\\.door"
    - from: "index.html hero Which door button and topnav access entry"
      to: "index.html #access"
      via: "native anchor jump onto a focusable section, with smooth scrolling and its reduced-motion reset already in styles.css"
      pattern: "href=\"#access\""
---

<objective>
Ship the door video section complete against an absent file (D-11), so it reads as
documentation pending rather than a broken player, and so the day the owner sets one config
line the player appears with no code change and no layout shift. Lock the inline playback
attribute pair that stops iOS Safari taking the video fullscreen (D-13, ACC-01), keep every
failure path landing on the same deliberate panel (ACC-05, D-19), close the jump path from the
top of the page to the door (D-20, ACC-02), and run the real device pass the phase owes (D-23).

Purpose: this is the section the whole site is named after. It may show its unconfigured state
for weeks, so that state is the deliverable, not a stopgap.

Output: `buildVideo()`, a fixed ratio `.video-slot` that holds either the player or the pending
panel, a back to map inline link, focusable jump targets, and the closing verification sweep for
the phase.
</objective>

## Artifacts this phase produces

Full phase registry. **Own** marks the plan that creates the symbol.

| Symbol | Kind | Own |
|---|---|---|
| `pendingBlock(titleKey, bodyKey)` | app.js function, returns a `.pending` node | 01 |
| `renderLocation()` | app.js zero-arg renderer | 01 |
| `wireLocation()` | app.js one-shot listener wiring, called from `init()` | 01 |
| `isApplePlatform()` | app.js boolean helper | 01 |
| `directionsUrls(address)` | app.js helper, returns an object with google and apple URLs | 01 |
| `renderMapSlot()` | app.js renderer for `#loc-map` | 02 |
| `mountMap(slot)` | app.js one-shot frame mount | 02 |
| `renderAccess()` | app.js zero-arg renderer | 03 |
| `subHeading(key)` | app.js helper, returns an `h3.sub-h` node | 03 |
| `buildDirections()` | app.js helper, returns the written directions node | 03 |
| `buildNotes()` | app.js helper, returns the notes list node or null | 03 |
| `buildVideo()` | app.js helper, returns the player or the pending panel | 04 |
| `#loc-data` | element id, child of `#location-body`, rebuilt every render | 01 |
| `#loc-map` | element id, sibling of `#loc-data`, created once | 02 |
| `.addr` `.addr__label` `.addr__value` `.addr__note` | CSS | 01 |
| `.dirs` `.copybtn` | CSS | 01 |
| `.map-slot` `.map-wait` `.map-wait__bar` `.map-wait__line` | CSS | 02 |
| `.sub-h` `.steps` `.dir-prose` `.facts--notes` | CSS | 03 |
| `.video-slot` `.inline-link--back` | CSS | 04 |
| `data-state` on `.copybtn` (`copied`, `failed`) | attribute | 01 |
| `data-state` on `.map-slot` (`mounting`, `ready`, `blocked`) | attribute | 02 |
| `data-show` on the mounted map frame | attribute | 02 |
| `data-orient="portrait"` and `--video-aspect` on `.video-slot` | attribute plus custom property | 04 |
| `tabindex="-1"` on `#location` and `#access` | attribute | 04 |
| `venue.address` value set | config.js | 01 |
| `venue.notes` object, seven fixed null keys | config.js | 03 |
| `door.aspect`, default 16/9 | config.js | 04 |
| `loc.lede` `loc.address` | copy.js keys | 01 |
| `loc.copied.toast` `loc.copy.failed` `loc.copy.failed.toast` | copy.js keys | 01 |
| `loc.map.loading` `loc.map.blocked` | copy.js keys | 02 |
| `access.dir.heading` `access.dir.pending.title` `access.dir.pending.body` | copy.js keys | 03 |
| `access.notes.heading` plus `notes.entrance` through `notes.arrive` | copy.js keys | 03 |
| `access.video.heading` `access.back` | copy.js keys | 04 |

**Running copy.js key count per table:** 111 on entry, 112 after task 1, 113 after task 3.
Twenty new keys across the phase, in three tables at identical key sets.

<execution_context>
@D:/04. Hoobies & Passions/14. Random Websites/01. Master's Party/.claude/gsd-core/workflows/execute-plan.md
@D:/04. Hoobies & Passions/14. Random Websites/01. Master's Party/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/DESIGN-BRIEF.md
@.planning/phases/02-practical-information/02-CONTEXT.md
@.planning/phases/02-practical-information/02-UI-SPEC.md
@.planning/phases/02-practical-information/02-PATTERNS.md
@.planning/phases/02-practical-information/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The video slot and the unconfigured state that ships first</name>
  <files>config.js, copy.js, app.js, styles.css</files>
  <read_first>
    - `renderAccess()`, `subHeading()` and `pendingBlock()` from plans 01 and 03, and the 02-03 SUMMARY
    - `config.js` lines 47 to 63, the THE DOOR block, for the comment frame, the second-person voice and the capitalised imperative idiom used for the one thing the owner will actually do
    - `styles.css` lines 501 to 523, the `.pending` panel, whose padding and maximum width the in-slot variant overrides
    - `02-UI-SPEC.md` Interaction States sections 7 and 8, which fix the in-slot pending geometry, the aspect custom property and the portrait cap, and the reason the panel fills the slot rather than floating inside it
    - `02-CONTEXT.md` D-11, D-14, D-15, D-18, D-19
  </read_first>
  <action>
    Add `aspect` to the `door` object in config.js with the default value 16/9, written as a
    string. Document it in the file's voice for someone who is not a programmer: if you filmed it
    upright on your phone, change this to 9/16. Extend the THE DOOR comment block so it states in
    one capitalised imperative that dropping the file into assets and setting `videoSrc` is the
    whole job, and keep the existing sentence about what null does. Do not change `directions`,
    which plan 03 already documented.

    Add `access.video.heading` to all three tables in copy.js inside the access cluster, at the
    same index in each, using the exact string from the UI-SPEC Copywriting Contract.

    Add `buildVideo()` to app.js. It reads `var door = CFG.door || {};` and returns a slot node
    in every case, so the section height is identical whether or not a file exists. Create a div
    with class `video-slot`. Parse `door.aspect`, defaulting to 16/9 when it is missing or
    unparseable, set it as an inline custom property named for the video aspect on the slot, and
    when the numerator is smaller than the denominator also set a portrait orientation attribute
    on the slot. This is deterministic, needs no viewport arithmetic, and holds for both the
    player and the pending panel because they share the slot.

    When `door.videoSrc` is null or empty, place a single node inside the slot: the result of
    `pendingBlock('access.pending.title', 'access.pending.body')`. Both strings already exist in
    all three languages from phase 1 and are reused unchanged; re-translating them would be
    churn. Construct no video element at all in this branch.

    In `renderAccess()`, append `subHeading('access.video.heading')` and then the result of
    `buildVideo()`, after the practical notes block, matching the fixed order in D-15. Unlike the
    written directions and the notes, this heading and this slot are always present, because the
    slot always renders something at the player's ratio.

    In styles.css add the slot rules. `.video-slot` is full width with a 760px maximum, centred,
    with its aspect ratio taken from the custom property and falling back to 16/9, and with
    `var(--s-7)` above it to match the spacing between the section's sub-blocks. The portrait
    orientation attribute caps the maximum width at 360px, because a 9 by 16 clip at 760px wide
    would be taller than most phone screens.

    The pending panel inside the slot takes full height and drops its own maximum width, keeping
    its existing padding and top-aligning its content, so it fills the slot rather than floating
    centred inside it. A centred card inside a bordered box is a nested card, which is always
    wrong: one box, one border, one panel. Do not add a border to the slot itself in the pending
    case, since the panel already carries one.
  </action>
  <verify>
    <automated>node --check app.js &amp;&amp; node -e "global.window={};require('./config.js');var d=window.PARTY_CONFIG.door||{};if(d.aspect!=='16/9'){console.error('door.aspect should default to 16/9, got '+JSON.stringify(d.aspect));process.exit(1)}if(d.videoSrc!==null||d.posterSrc!==null){console.error('video and poster should still ship null');process.exit(1)}console.log('door config OK')" &amp;&amp; node -e "global.window={};require('./copy.js');var C=window.PARTY_COPY;var en=Object.keys(C.en).sort().join('|');['it','da'].forEach(function(l){if(Object.keys(C[l]).sort().join('|')!==en){console.error('key set mismatch in '+l);process.exit(1)}});var n=Object.keys(C.en).length;if(n!==112){console.error('expected 112 keys per table, got '+n);process.exit(1)}console.log('copy parity OK 112x3')" &amp;&amp; grep -qF 'function buildVideo(' app.js &amp;&amp; grep -qF 'buildVideo()' app.js &amp;&amp; grep -qF 'access.pending.title' app.js &amp;&amp; grep -qF '.video-slot' styles.css &amp;&amp; grep -qF 'video-aspect' styles.css &amp;&amp; grep -qF 'data-orient="portrait"' styles.css &amp;&amp; echo SLOT_GATE_PASS</automated>
    <human-check>
      <test>With door.videoSrc null, load the preview and read Building access at phone width. Measure the panel height, then set door.aspect to 9/16 and reload.</test>
      <expected>A Door video heading over one filled panel at a wide landscape ratio, titled Access documentation pending, its text top aligned inside the panel with a single border around it and no nested box. At 9 by 16 the same panel becomes tall and narrow, capped at 360px wide and centred, still one box.</expected>
      <why_human>Aspect ratio geometry and the panel filling rather than floating are visual outcomes with no DOM harness in this project.</why_human>
    </human-check>
  </verify>
  <done>The access section ends with a heading and a fixed ratio panel that reads as documentation pending, at whichever ratio the owner configured.</done>
  <acceptance_criteria>
    - `config.js` loads under Node with `door.aspect` equal to 16/9 and both `videoSrc` and `posterSrc` still null
    - the THE DOOR comment tells the owner what to change if the clip was filmed upright
    - `copy.js` holds exactly 112 keys per table with identical key sets
    - `app.js` contains `function buildVideo(` and `renderAccess()` calls it after the notes block
    - the null branch of `buildVideo()` constructs no video element: it appends only the result of `pendingBlock(`
    - `styles.css` contains `.video-slot` with an aspect-ratio driven by a custom property and a portrait attribute selector capping the width
    - the in-slot pending rule sets full height and removes the panel maximum width, and adds no second border
    - `node --check app.js` exits 0 and the ES5 gates return 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: The player, its attribute pair, and every failure landing on the same panel</name>
  <files>app.js, styles.css</files>
  <read_first>
    - `buildVideo()` from task 1
    - `app.js` lines 21 to 30, the `store` wrapper, for the discipline of degrading silently to a working fallback rather than surfacing a browser error
    - the module-scope preserved-node rule established in plan 03 task 1, and its SUMMARY
    - `02-UI-SPEC.md` Interaction States section 6, including the full attribute list, the refusal to restyle native controls, the reason an empty poster attribute is forbidden and the reason a 404 reuses the pending copy
    - `02-CONTEXT.md` D-11, D-13, D-14, D-19
  </read_first>
  <action>
    Extend `buildVideo()` so that when `door.videoSrc` is a non-empty string it constructs a
    video element with document.createElement and fills the slot with it.

    Attributes, all mandatory and locked by D-13 and ACC-01: playsinline, muted, controls, and
    preload set to metadata. Set playsinline and muted both as attributes and as element
    properties, because older WebKit honours the property where a late attribute set is ignored,
    and because either one alone fails inline playback on iOS Safari. They are a pair and are
    asserted as a pair. The element must never begin playback by itself: do not add the
    self-starting attribute that D-13 and UI-SPEC section 6 both forbid, do not name it in a
    comment either, and never call play from script.

    Set the poster attribute only when `door.posterSrc` is a non-empty string. When it is null,
    omit the attribute entirely rather than setting it to an empty string: an empty poster makes
    Safari request the page itself as an image.

    Set the source as the src attribute on the video element itself rather than as a child source
    element, so a failed load fires an error event on the element the handler is attached to.

    Attach the error handler at construction time, on the element being constructed, and only
    there. This is the sanctioned exception to the rule that listeners live in a wire function:
    the element is created once and held in a module-scope reference, so the listener is attached
    exactly once and cannot stack. On error, replace the slot contents with
    `pendingBlock('access.pending.title', 'access.pending.body')`, the same panel the null branch
    produces, and record that the mounted source failed so a later re-render does not re-append a
    known-broken element. No browser error text, no developer-facing message and nothing written
    to the console ever reaches the guest: to a guest a missing file and an unmade file are the
    same event and the same correct message. The owner catches the difference from the config
    file, not from the guest-facing page.

    Hold the constructed element and the source string it was built from in module-scope
    variables, following the preserved-node rule established in plan 03. When `renderAccess()`
    runs again on a language switch and the configured source is unchanged and the element has
    not errored, re-append the existing element into the freshly built slot rather than
    constructing a new one, so playback is not interrupted and metadata is not refetched. When
    the configured source has changed, discard the reference and build fresh.

    In styles.css make the video element fill the slot as a block at full width and height, with
    the surface token as its background and object-fit contain, so letterboxing bars and any
    unpainted frame are institutional grey rather than black. This is the concrete answer to
    ACC-03 when no poster exists. Do not restyle the native controls in any way: a hand-rolled
    player is pure failure surface on iOS Safari and the controls attribute is required. Do not
    set the download-suppressing control list attribute, which is Chrome only and serves nobody
    here. The global focus-visible ring applies to the element unchanged.
  </action>
  <verify>
    <automated>node --check app.js &amp;&amp; grep -qF "createElement('video')" app.js &amp;&amp; grep -qF 'playsinline' app.js &amp;&amp; grep -qF 'playsInline' app.js &amp;&amp; grep -qF 'preload' app.js &amp;&amp; grep -qF 'controls' app.js &amp;&amp; test $(grep -c 'autoplay' app.js) -eq 0 &amp;&amp; test $(grep -cE "setAttribute\('poster',[[:space:]]*''\)" app.js) -eq 0 &amp;&amp; test $(grep -cE "setAttribute\('controlsList'" app.js) -eq 0 &amp;&amp; test $(grep -c 'console.log' app.js) -eq 0 &amp;&amp; test $(grep -c 'console.error' app.js) -eq 0 &amp;&amp; grep -qF 'object-fit' styles.css &amp;&amp; grep -qF 'video-slot video' styles.css &amp;&amp; echo PLAYER_GATE_PASS</automated>
    <human-check>
      <test>Put any small mp4 in assets and set door.videoSrc to it. Load the preview on real iOS Safari and press play. Then set door.videoSrc to a path that does not exist and reload. Then, with the working file, start playback and switch language mid-play.</test>
      <expected>iOS Safari plays the clip inline inside the slot with native controls and no fullscreen takeover. The bad path shows the same Access documentation pending panel a null source shows, with no browser error text and nothing in the console for a guest to find. Switching language mid-play does not restart or reload the video and the heading above it changes language.</expected>
      <why_human>Inline playback with no fullscreen takeover is the single behaviour this phase most needs and it cannot be proven without a real iOS device. D-23 already mandates the pass.</why_human>
    </human-check>
  </verify>
  <done>A configured video plays inline on iOS Safari, a broken path lands on the same deliberate panel as a missing one, and a language switch never interrupts playback.</done>
  <acceptance_criteria>
    - `app.js` sets playsinline and muted both as attributes and as element properties, in the same construction path, so neither can ship without the other
    - `app.js` sets controls and preload metadata, and `grep -c 'autoplay' app.js` returns 0
    - `grep -cE "setAttribute\('poster',[[:space:]]*''\)" app.js` returns 0, and the poster attribute is set only inside a truthiness branch on `door.posterSrc`
    - the source is set on the video element itself, and an error listener is attached to that element at construction
    - the error handler replaces the slot contents with the result of `pendingBlock(` and writes nothing to the console: `grep -c 'console.log' app.js` and `grep -c 'console.error' app.js` both return 0
    - `grep -cE "setAttribute\('controlsList'" app.js` returns 0
    - `styles.css` styles the video element inside the slot with object-fit contain and the surface background, and contains no rule targeting a native control pseudo-element
    - `node --check app.js` exits 0 and the ES5 gates return 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Back to the map, focusable jump targets, and the closing verification sweep</name>
  <precondition>node tools/preview.js serves the working tree at http://127.0.0.1:4173, reachable from the phone used for the device pass or over the same LAN</precondition>
  <files>copy.js, app.js, styles.css, index.html</files>
  <read_first>
    - `renderAccess()` and its fixed block order from plan 03
    - `index.html` lines 109 to 112 (the hero Which door button) and 54 to 61 (the topnav, whose access entry is the one item kept on phones)
    - `styles.css` lines 56 to 61 and 674 to 686, the existing smooth scrolling with its scroll padding and the reduced-motion reset that already covers it
    - `styles.css` lines 561 to 567, `.inline-link`, and lines 230 to 235, the coarse-pointer touch target block
    - `02-UI-SPEC.md` Interaction States section 11 and the two Layout Contract discretion calls, plus the Executor Checklist and the Verification section
    - `02-CONTEXT.md` D-20, D-21, D-22, D-23
  </read_first>
  <action>
    Add `access.back` to all three tables in copy.js, at the same index in each, using the exact
    string from the UI-SPEC Copywriting Contract. The link text stands alone and reads correctly
    out of context, which a bare Back would not.

    In `renderAccess()`, append last an anchor with classes `inline-link inline-link--back`,
    href pointing at the location section, and text `t('access.back')`. It is an inline link
    rather than a button: it is a within-page jump, not an action, the page already uses this
    component for exactly this purpose in the fact table, and a third large button in a section
    that already carries a video would compete with the two directions buttons that are the real
    actions on the page.

    In index.html add a tabindex of minus one to the location section and to the access section,
    so a native anchor jump moves focus onto the landed section for keyboard and screen reader
    users rather than only moving the viewport. Change nothing else in the markup. Add no JS
    scroll handler of any kind: the existing smooth scrolling with its top scroll padding is
    already reset to an instant jump inside the reduced-motion query, and a scripted scroll would
    bypass that preference. No new pinned or fixed element is added either, because the bottom
    bar is reserved for the enrollment nudge and a second fixed element would fight it on a
    phone.

    In styles.css give `.inline-link--back` an inline-flex box with a 44px minimum height inside
    the existing coarse-pointer block so it is tappable, and clear the left margin the base inline
    link carries, since this one begins its own line rather than trailing a sentence. Add a rule
    that suppresses the focus ring only for a programmatically focused section, matching on focus
    but not focus-visible, so a mouse-driven jump does not paint a ring around a whole section
    while a keyboard user still gets one. Never remove a ring without leaving the keyboard case
    intact.

    Then run the phase closing sweep and record the outcome in the plan SUMMARY. Confirm across
    the five source files that there are zero em dashes and zero en dashes; that the markup
    contains no frame element and no address; that the enrollment form id still appears exactly
    twice in app.js and `enrollmentReady()` is unchanged, so the nudge bar stays down; that the
    faint ink token count in styles.css is unchanged at four, so no new element sits on a 3.88:1
    pairing; and that every copy table still holds identical key sets at 113 keys.
  </action>
  <verify>
    <automated>node --check app.js &amp;&amp; node -e "global.window={};require('./copy.js');var C=window.PARTY_COPY;var en=Object.keys(C.en).sort().join('|');['it','da'].forEach(function(l){if(Object.keys(C[l]).sort().join('|')!==en){console.error('key set mismatch in '+l);process.exit(1)}});var n=Object.keys(C.en).length;if(n!==113){console.error('expected 113 keys per table, got '+n);process.exit(1)}console.log('copy parity OK 113x3')" &amp;&amp; grep -qF 'inline-link--back' app.js &amp;&amp; grep -qF 'inline-link--back' styles.css &amp;&amp; test $(grep -c 'tabindex="-1"' index.html) -eq 2 &amp;&amp; test $(grep -c 'scrollIntoView' app.js) -eq 0 &amp;&amp; test $(grep -c '<iframe' index.html) -eq 0 &amp;&amp; test $(grep -c 'enrol-form' app.js) -eq 2 &amp;&amp; test $(grep -c -- '--ink-faint' styles.css) -eq 4 &amp;&amp; node -e "var fs=require('fs');['index.html','styles.css','app.js','config.js','copy.js'].forEach(function(f){if(/[\u2014\u2013]/.test(fs.readFileSync(f,'utf8'))){console.error('em or en dash found in '+f);process.exit(1)}});console.log('dash scan clean')" &amp;&amp; test $(grep -cE '\.innerHTML[[:space:]]*=' app.js) -eq 0 &amp;&amp; test $(grep -c '=>' app.js) -eq 0 &amp;&amp; test $(grep -cE '^[[:space:]]*(const|let)[[:space:]]+[A-Za-z_$]' app.js) -eq 0 &amp;&amp; echo PHASE_SWEEP_PASS</automated>
    <human-check>
      <test>Device pass, D-23, on real hardware rather than emulation. On a real iOS Safari and a real Android Chrome, open the site and work through: tap Which door in the hero, then the Building access entry in the top bar; press play on the video if a file is configured; tap Copy address and paste it somewhere; tap Open in Google Maps and, on iOS, Open in Apple Maps; tap Back to the map and address. Then set the viewport to 320px wide in DA with all seven notes filled. Then repeat the hero jump with Reduce Motion enabled and again using only the keyboard.</test>
      <expected>Both jumps land on the access section with its heading below the sticky top bar rather than under it. Video plays inline with no fullscreen takeover. The pasted address is exact, including the a-ring and the o-slash. Google Maps opens a route on both platforms, Apple Maps opens a route on iOS, and the Apple button is absent on Android. The back link returns to the location section and is at least 44px tall. At 320px in Danish nothing scrolls sideways and no label truncates. With Reduce Motion the jump is instant rather than animated. With the keyboard, focus lands on the jumped-to section so the next Tab continues inside it.</expected>
      <why_human>Real device browsers, real touch targets and real assistive behaviour cannot be proven from source, and D-23 makes this pass a condition of closing the phase.</why_human>
    </human-check>
  </verify>
  <done>The access section ends with a working back link, both jump entry points land with focus on a focusable target, and the phase closing sweep passes on every source gate.</done>
  <acceptance_criteria>
    - `copy.js` holds exactly 113 keys per table with identical key sets across en, it and da
    - `app.js` appends an anchor carrying `inline-link--back` as the last block of `renderAccess()`
    - `index.html` contains exactly two occurrences of a minus one tabindex, on the location and access sections
    - `grep -c 'scrollIntoView' app.js` returns 0: no scripted scroll bypasses the reduced-motion preference
    - `styles.css` gives the back link a 44px minimum height on coarse pointers and suppresses the ring only for the focus-without-focus-visible case
    - `grep -c '<iframe' index.html` returns 0 and the markup contains no address string
    - `grep -c 'enrol-form' app.js` returns exactly 2 and `enrollmentReady()` is byte-identical to its phase 1 body
    - `grep -c -- '--ink-faint' styles.css` returns 4
    - zero em dashes and zero en dashes across index.html, styles.css, app.js, config.js and copy.js
    - `node --check app.js` exits 0, `grep -c '=>' app.js` returns 0, `grep -cE '^[[:space:]]*(const|let)[[:space:]]+[A-Za-z_$]' app.js` returns 0, `grep -cE '\.innerHTML[[:space:]]*=' app.js` returns 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.js to media element | `door.videoSrc` and `door.posterSrc` are owner-authored strings applied as element attributes. |
| assets directory to page | The video file is a same-origin relative asset committed to a public repository. |
| browser error surface to guest | A failed media load produces browser-generated state that must never reach the guest as raw text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Tampering | `door.videoSrc` and `door.posterSrc` applied as attributes on a created media element | medium | mitigate | Attributes set with setAttribute on an element built by createElement, never through markup assignment. Both values are same-origin relative paths under assets. The poster attribute is omitted entirely when null rather than set empty, which also removes the case where Safari requests the page itself as an image. |
| T-02-12 | Spoofing | A missing, moved or unsupported video surfacing browser or developer text where a guest expects instructions | low | mitigate | The error handler replaces the slot contents with the same pending panel the null branch produces. Gate: zero console calls anywhere in app.js, so no developer-facing string exists to leak. |
| T-02-13 | Information disclosure | A door video filmed at a private residence, committed to a public repository and served publicly | medium | accept | The same accepted owner decision as the address and the notes, recorded in CONTEXT.md. The file ships null and nothing is exposed until the owner commits a clip. Listed so the acceptance is visible rather than an omission. |
| T-02-SC | Tampering | Dependency supply chain | low | accept | No package manager, no lockfile, no build step, nothing installed by this plan or by this phase. The only third-party runtime asset in the phase is the Google Maps embed, modelled in plan 02. |
</threat_model>

<verification>
Phase closing sweep, all run from the repository root:

1. `node --check app.js` exits 0.
2. copy.js parity harness reports 113 keys across three identical key sets.
3. `grep -c '<iframe' index.html` returns 0.
4. `grep -c 'enrol-form' app.js` returns exactly 2.
5. `grep -c -- '--ink-faint' styles.css` returns 4.
6. `grep -cE '\.innerHTML[[:space:]]*=' app.js` returns 0.
7. Zero em dashes and zero en dashes in all five source files.
8. `grep -c 'scrollIntoView' app.js` returns 0.

The D-23 device pass and the 320px Danish pass are the phase's two backstop items. They abstain
to human review rather than passing silently, because this project has no package manager and no
build step and therefore no device or DOM harness, and adding one to satisfy a gate would break
a locked project constraint.
</verification>

<success_criteria>
- The access section reads as documentation pending, at the right ratio, with no file present.
- Setting one config line produces a player that plays inline on iOS Safari with no fullscreen
  takeover, and no code changes.
- Every failure path, absent file or broken path, lands on the same deliberate panel.
- Both jump entry points land on the access section with focus, instantly under reduced motion.
- The phase closing sweep passes on every source gate, and the device pass is recorded.
</success_criteria>

<output>
Create `.planning/phases/02-practical-information/02-04-SUMMARY.md` when done.
</output>
