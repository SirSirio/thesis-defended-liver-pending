---
phase: 04-photos
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - supabase/schema.sql
  - .planning/phases/04-photos/04-DEVICE-PASS.md
autonomous: false
requirements: [PH-01, PH-02, PH-03, PH-05, PH-06, PH-07, DEL-02, DEL-03, DSG-05, DSG-08]
user_setup:
  - service: supabase
    why: "Applying DDL and removing rows and objects, neither of which any credential in this repository can do"
    dashboard_config:
      - task: "Run supabase/schema.sql in the SQL editor, whole file, once"
        location: "Supabase dashboard, project aplaxdplwnnlezffatal, SQL Editor, New query"
      - task: "Delete every photos row whose name is ZZTEST DeleteMe, and delete the zz-research folder from the party-photos bucket"
        location: "Supabase dashboard, Table Editor and Storage"
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The party-photos bucket record carries a byte-counted size ceiling of 3145728 and a declared-type allow-list of image/jpeg, and the insert that sets them updates an existing bucket rather than skipping it, because the bucket already exists (D-24)."
    - "The honest framing survives into the schema comment: the size ceiling is counted on real bytes and therefore holds against a crafted request, while the type allow-list is validated against the declared content type and is hygiene rather than a wall. Neither is described as something it is not."
    - "supabase/schema.sql remains safe to run more than once, and the whole file is run rather than only the changed section, so the earlier sections are proved again on the way past."
    - "The header STATUS block records that the sixth-photo refusal, which the file called unproven, is now proven on the wire with HTTP 400 and the raise-exception code, and no longer describes it as outstanding."
    - "Nothing in this phase adds a read rule or a delete rule to any table or to storage: grep for a delete policy in the whole file returns zero, and the photos table gains no select policy, so the raw enrollments and photos tables stay unreadable to the publishable key."
    - "The bucket change is proved by attempting a real operation and observing the resulting state, never by an HTTP status alone, because on this project a blocked read answers with an empty array and a blocked delete answers 204 and both look like success."
    - "The research session's six rows and eight objects are removed by the owner, and the removal is proved through public.album rather than through the blocked photos table."
    - "04-DEVICE-PASS.md exists and records every D-30 line as a checked observation on a real phone, in the shape phases 2 and 3 used, including the one claim in this phase that only a real device can settle."
    - statement: "UI consideration, cross-cutting overflow: the album grid, the queue and the control all render correctly at 320px width, in Danish (D-30)."
      verification: backstop
    - statement: "UI consideration, cross-cutting long-text: a portrait iPhone photograph lands the right way up in the album, and an HEIC pick from the iOS camera roll arrives as a JPEG. The single most visible way this phase can fail, and the one claim in the design contract that only a real device can settle (D-17 as refined, D-30)."
      verification: backstop
    - statement: "UI consideration, cross-cutting loading: backgrounding the tab mid-upload on iOS does not leave the control stranded in the uploading state. The 60 second timeout or an error handler terminates it and the row lands in the failed state with a retry offered."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT make the raw enrollments or photos tables readable to the publishable key. Both carry a guest_id, which is a bearer write credential for a registration, and the free-text note is readable by the host and by nobody else. Every public read goes through a view whose projection is the boundary."
      category: privacy
      status: resolved
      verification: judgment
  artifacts:
    - path: "supabase/schema.sql"
      provides: "the bucket size ceiling and type allow-list, and a header that no longer calls the trigger unproven"
      contains: "allowed_mime_types"
    - path: ".planning/phases/04-photos/04-DEVICE-PASS.md"
      provides: "the D-30 record sheet, filled on a real phone"
      contains: "portrait"
  key_links:
    - from: "supabase/schema.sql §6"
      to: "the live party-photos bucket record"
      via: "an owner re-run in the SQL editor; there is no CLI, no migrations directory and no key in this repository that can apply DDL"
      pattern: "storage.buckets"
    - from: "supabase/schema.sql §4"
      to: "config.js photos.maxPerGuest"
      via: "a note saying the literal in the trigger and the config value must move together, in the same shape §10 already uses for the guest count bound"
      pattern: "maxPerGuest"
---

<objective>
Put the only real server-side control this phase has onto the bucket, get the owner to apply it and
to clear the artifacts three sessions of probing have left behind, and then take the whole thing to
a real phone, because the one claim this phase cannot settle from a terminal is whether a portrait
iPhone photograph lands the right way up.

Purpose: every client-side check in this phase protects a guest from their own mistake and an
attacker from nothing at all. What holds against a direct call to the API is the bucket record
itself, and it lives in Supabase rather than in git, so it is exactly the kind of change that ships
correct in the file and absent in the world unless somebody proves otherwise.

Output: `supabase/schema.sql` §6 rewritten with its two limits and its corrected rationale, the
header STATUS block updated to discharge the proof the file called outstanding, one blocking owner
handoff carrying both actions as one, and `04-DEVICE-PASS.md` filled in.
</objective>

## Probe ledger

**Edges.** This plan authors none of the phase's 10 authored edges and flags none. Phase accounting
is in `04-01-PLAN.md` `## Probe ledger`.

**UI considerations.** This plan lifts 3 of the phase's 83 rows: cross-cutting rows 5, 6 and 7, all
three of which are backstop items that only a real device can settle, which is why they land here.

**Prohibitions.** The last of the six recalled project prohibitions is authored here,
descriptor-less so it disposes flagged-unverified.

## Flagged planner assumptions

None new in this plan.

## Artifacts this phase produces

This plan's slice of the phase registry. The full registry is in `04-01-PLAN.md`.

| Symbol | Kind |
|---|---|
| `storage.buckets.file_size_limit = 3145728` on `party-photos` | schema, live service config |
| `storage.buckets.allowed_mime_types = {image/jpeg}` on `party-photos` | schema, live service config |
| `on conflict (id) do update` on the §6 bucket insert | schema |
| header STATUS paragraph discharging the sixth-photo proof | schema comment |
| §4 note tying the trigger's literal to `photos.maxPerGuest` | schema comment |
| `.planning/phases/04-photos/04-DEVICE-PASS.md` | new file |

<execution_context>
@D:/04. Hoobies & Passions/14. Random Websites/01. Master's Party/.claude/gsd-core/workflows/execute-plan.md
@D:/04. Hoobies & Passions/14. Random Websites/01. Master's Party/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-photos/04-CONTEXT.md
@.planning/phases/04-photos/04-RESEARCH.md
@.planning/phases/04-photos/04-UI-SPEC.md
@.planning/phases/04-photos/04-04-SUMMARY.md
@.planning/phases/03-enrollment-identity-and-the-group/03-DEVICE-PASS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section 6 gets the two limits, and the header stops calling the trigger unproven</name>
  <reversibility rating="costly">Undoing needs the owner back at the dashboard re-running the file with different values, which is a coordinated change rather than a local one. CONTEXT.md D-24 called it one-way in the sense that phase 3's schema changes were, but the bucket record is mutable and no migration and no broken contract exists, so the honest rating is costly and task 2 is the human gate.</reversibility>
  <files>supabase/schema.sql</files>
  <read_first>
    - `04-RESEARCH.md` `## Code Examples` E6 in full, the SQL body and its whole comment block, plus the two paragraphs under it on the units and the free-tier ceiling.
    - `04-RESEARCH.md` `## Security Domain`, `### Controls that hold against a crafted request` and `### Controls that do NOT hold, and must not be described as if they do`, both tables in full.
    - `04-RESEARCH.md` `## THE PROOF` in full, which is what the header STATUS block now has to record.
    - `04-RESEARCH.md` `## Common Pitfalls` Pitfall 9, the dashboard folder-creation surprise the owner is the only person who will ever meet.
    - `04-RESEARCH.md` `## Existing Code`, the closing note on the trigger's literal and `photos.maxPerGuest`.
    - `04-CONTEXT.md` D-24, D-26, D-27.
    - `supabase/schema.sql` in full. Its header comment is the file's own changelog and the register every new comment must match; §3 is the honest threat model; §4 is the trigger and why it carries owner rights; §6 is what changes; §7 is the idempotent shape to copy; §9 is the album view and the naming warning; §10 is the pattern for tying a database bound to a config value.
  </read_first>
  <action>
    Rewrite §6 of `supabase/schema.sql` using research E6 as the body, verbatim including its comment
    block. The bucket insert gains `file_size_limit` set to 3145728, which is three mebibytes in
    bytes at the SQL layer, and `allowed_mime_types` set to an array holding the single JPEG type.
    Its conflict clause changes from the non-updating form to one that updates the public flag, the
    size limit and the type list from the excluded row, in the same idempotent shape §7 already uses
    for the withdrawn column. The non-updating form never touches a bucket that already exists, and
    this bucket already exists, which is why the change is necessary at all.

    The comment must carry the honest framing and must not soften it. State that the size limit is
    counted on the bytes that arrive and therefore holds against anything, and that the type list is
    checked against the type the uploader declares rather than against the bytes, so it stops
    accidents and casual junk and not a deliberate request. State the number in human terms: roughly
    ten times what the site produces after it shrinks a photograph, and small enough that nobody
    fills the free tier. Add the one line an owner needs before they meet it: with a type list set,
    creating a folder through the dashboard can be refused, because the empty placeholder file it
    creates is not a JPEG.

    Update the header STATUS block. Its closing paragraph currently records the sixth-photo refusal
    as the one thing about this database that has never been executed, and hands that proof to phase
    4 as an explicit task. Replace that paragraph with what happened: six inserts
    were executed against this project on 2026-08-15 under a throwaway identity with the name marker
    the owner already recognises; the first five were accepted at HTTP 201 with empty bodies and the
    sixth was refused at HTTP 400 with the raise-exception code and the trigger's literal message;
    all five were read back through `public.album` immediately afterwards, which is what makes it
    proof rather than a status code; and a seventh insert under a different identity was accepted,
    which confirms the limit is per identity rather than global. Add the ordering fact the same probe
    surfaced: the trigger fires before the unique constraint, so a guest at the limit receives the
    same refusal code for a path collision, which is why the client treats that code as being at the
    limit unconditionally and never retries. Then add a new paragraph in the same register recording
    that §6 was changed on this date and what it now carries.

    Add a short note to §4, in the shape §10 already uses for the guest count bound: the literal in
    the trigger and `photos.maxPerGuest` in `config.js` agree today and must move together, and if
    the config value is ever raised alone the database refuses the sixth anyway.

    Do not add a select rule to any table. Do not add a delete rule to anything. Do not change §9's
    revoke, which is safe to keep only while §4 carries owner rights, and do not touch §4's owner
    rights. **This task does not claim the change is applied.** The file is written; task 2 gets it
    run.
  </action>
  <verify>
    <automated>grep -c 'allowed_mime_types' supabase/schema.sql | grep -qx 2 &amp;&amp; grep -c 'file_size_limit' supabase/schema.sql | grep -qx 2 &amp;&amp; echo "both limits present in the insert and the update"</automated>
    <automated>grep -c '3145728' supabase/schema.sql | grep -qx 1 &amp;&amp; echo "size limit is in bytes"</automated>
    <automated>test "$(grep -c 'do nothing' supabase/schema.sql)" = 0 &amp;&amp; grep -q 'do update' supabase/schema.sql &amp;&amp; echo "the bucket insert now updates an existing record"</automated>
    <automated>test "$(grep -c 'for delete' supabase/schema.sql)" = 0 &amp;&amp; test "$(grep -c 'public.photos for select' supabase/schema.sql)" = 0 &amp;&amp; test "$(grep -c 'security_invoker' supabase/schema.sql)" = 0 &amp;&amp; echo "no delete rule, no read rule on photos, no invoker views"</automated>
    <automated>grep -q 'security definer' supabase/schema.sql &amp;&amp; grep -q 'revoke select on public.photos from anon' supabase/schema.sql &amp;&amp; echo "the trigger keeps owner rights and the revoke is intact"</automated>
    <automated>test "$(grep -c 'still unproven' supabase/schema.sql)" = 0 &amp;&amp; grep -q 'P0001' supabase/schema.sql &amp;&amp; echo "the header records the proof rather than calling it outstanding"</automated>
    <automated>node -e "var s=require('fs').readFileSync('supabase/schema.sql','utf8');if(/[\u2013\u2014]/.test(s))throw new Error('en or em dash found');console.log('dash scan clean')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'allowed_mime_types' supabase/schema.sql` returns 2 and `grep -c 'file_size_limit' supabase/schema.sql` returns 2, once in the column list and once in the update set
    - `grep -c '3145728' supabase/schema.sql` returns 1, and the value is a bare integer rather than a string form
    - `grep -c 'do nothing' supabase/schema.sql` returns 0 and the file contains an updating conflict clause
    - `grep -c 'for delete' supabase/schema.sql` returns 0, `grep -c 'public.photos for select' supabase/schema.sql` returns 0, `grep -c 'security_invoker' supabase/schema.sql` returns 0
    - `grep -c 'security definer' supabase/schema.sql` returns 2, the photo limit trigger and the amend function, both unchanged
    - `grep -c 'revoke select on public.photos from anon' supabase/schema.sql` returns 1
    - `grep -c 'still unproven' supabase/schema.sql` returns 0 and the header contains the raise-exception code and the words `read back`
    - the §6 comment states both that the size limit is counted on arriving bytes and that the type list is checked against the declared type, and describes neither as the other
    - the §6 comment names the dashboard folder-creation consequence
    - §4 carries the note tying the trigger's literal to `photos.maxPerGuest`
    - zero em dashes and zero en dashes in the file
    - **This task does not claim the migration is applied.** The plan summary must say so explicitly
  </acceptance_criteria>
  <done>The file describes a bucket with a real byte ceiling and a declared-type list, says honestly which of the two is a control, and its header no longer calls the phase's central guarantee unproven.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] The owner runs the file and clears the artifacts, and a probe proves both</name>
  <action>
    Halt and hand off. Claude cannot apply DDL to this project and cannot delete a row or an object
    from it: there is no Supabase CLI in this repository, no migrations directory, and the service
    key was disabled by the owner after it was exposed. Present the instructions below verbatim, as
    one handoff with two parts, wait, then run the acceptance probes and read their output rather
    than accepting a report that the work was done.
  </action>
  <instructions>
    See `<how-to-verify>` below. In short: open the Supabase SQL editor for project
    `aplaxdplwnnlezffatal`, paste the whole of `supabase/schema.sql`, run it, then delete the test
    rows and the `zz-research` folder, then paste back the output of the three probes.
  </instructions>
  <verification>
    Three probes, and every one of them reads a resulting state rather than a status code, because on
    this project a blocked read answers with an empty array and a blocked delete answers 204 and both
    look like success.

    1. **The size ceiling holds.** A four megabyte upload of arbitrary bytes declared as JPEG is
       refused, and the object is then absent from the public read path. Before the re-run the same
       upload is accepted.
    2. **The type list holds against an accident.** An upload declaring a plain-text content type is
       refused. Before the re-run it is accepted.
    3. **The album is empty.** `GET /rest/v1/album?select=first_name,storage_path` returns `[]`.
       Read through the view, never through the blocked table, exactly as D-26 and D-27 require and
       exactly as the phase 3 cleanup was proved.
  </verification>
  <what-built>
    `supabase/schema.sql` §6 now carries a three mebibyte byte ceiling and a single-type allow-list on
    the `party-photos` bucket, applied with a conflict clause that updates an existing bucket instead
    of skipping it. The header records the sixth-photo proof that the file previously called
    outstanding, and §4 carries a note tying the trigger's literal to the config value. None of it is
    in the database yet, and the site works either way: an un-migrated bucket accepts anything, which
    is the state the site has shipped in since phase 3.
  </what-built>
  <how-to-verify>
    This is a genuine human-only step and it is the only one in the phase. Two actions, presented as
    one, because they are the same visit to the same dashboard.

    **Part one: apply the bucket limits.**

    1. Open the Supabase dashboard, project `aplaxdplwnnlezffatal`, then SQL Editor, then New query.
    2. Open `supabase/schema.sql` from the repository root, select the whole file, and paste it in.
       The whole file, not only section 6: it is written to be safe to run more than once, and
       running it whole is what keeps the file and the database honest with each other.
    3. Run it. Expect success with no rows returned.

    **Part two: remove what the probing left behind.**

    4. Still in the SQL editor, run `delete from public.photos where name = 'ZZTEST DeleteMe';`
       This removes the six rows the research session wrote to prove the five-photo trigger, plus any
       rows written from a terminal during plans 04-01 to 04-04. Then delete any row that belongs to
       a test upload made from a browser during this phase, which the summaries of plans 04-01 to
       04-04 list by `storage_path`.
    5. Go to Storage, then `party-photos`, and delete the whole `zz-research` folder. It holds eight
       objects: `probe-post.jpg`, `probe-cache.jpg`, `probe-cors.jpg`, `probe-spoof.jpg`,
       `probe-not-an-image.txt`, `probe-html.html`, `probe-svg.svg`, `probe-noct.bin`. Then delete
       the dated folders holding any browser test uploads from this phase.
    6. If the dashboard refuses to create a folder afterwards, that is expected and it is written in
       the section 6 comment: with a type list set, the empty placeholder file a new folder creates
       is not a JPEG. It does not affect deleting.

    **Then paste back the output of these three probes, run from the repository root.**

    ```
    U=$(node -e "global.window={};require('./config.js');process.stdout.write(window.PARTY_CONFIG.photos.supabaseUrl)")
    K=$(node -e "global.window={};require('./config.js');var p=window.PARTY_CONFIG.photos;process.stdout.write(p.supabaseKey||p.supabaseAnonKey)")

    # 1. size ceiling: 4MB of bytes declared as jpeg
    head -c 4194304 /dev/urandom > /tmp/big.jpg
    curl -sS -w '\nHTTP %{http_code}\n' -X POST "$U/storage/v1/object/party-photos/zz-gate/big.jpg" \
      -H "apikey: $K" -H "Content-Type: image/jpeg" --data-binary @/tmp/big.jpg
    curl -sS -o /dev/null -w 'public read of big.jpg: HTTP %{http_code}\n' \
      "$U/storage/v1/object/public/party-photos/zz-gate/big.jpg"

    # 2. declared type list
    printf 'not an image' > /tmp/plain.txt
    curl -sS -w '\nHTTP %{http_code}\n' -X POST "$U/storage/v1/object/party-photos/zz-gate/plain.txt" \
      -H "apikey: $K" -H "Content-Type: text/plain" --data-binary @/tmp/plain.txt

    # 3. the album is empty
    curl -sS "$U/rest/v1/album?select=first_name,storage_path" -H "apikey: $K"
    ```

    Expected after the re-run and the cleanup: probe 1 is refused and the public read of that object
    returns 400, probe 2 is refused, and probe 3 prints exactly `[]`. If probe 3 prints rows, the
    cleanup is incomplete and the remaining `storage_path` values name what is left.
  </how-to-verify>
  <resume-signal>Paste the output of all three probes. Type "applied" only alongside that output.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: The device pass, on a real phone, because a terminal cannot answer the orientation question</name>
  <precondition>The site is reachable on a phone: either `node tools/preview.js` is running and the phone is on the same network at 127.0.0.1:4173 through the host's LAN address, or the phase's commits are pushed and the live GitHub Pages URL serves the current code.</precondition>
  <files>.planning/phases/04-photos/04-DEVICE-PASS.md</files>
  <read_first>
    - `04-UI-SPEC.md` `## Executor Checklist` → Verification (D-30, `04-DEVICE-PASS.md`), every line. That list is the record sheet's contents.
    - `04-CONTEXT.md` D-17, D-24, D-28, D-29, D-30.
    - `04-RESEARCH.md` `## THE ORIENTATION REFINEMENT` and `## The Image Pipeline` Facts 1, 2 and 4, and `## Assumptions Log` rows A1 and A2, which are the two claims this sheet exists to settle.
    - `.planning/phases/03-enrollment-identity-and-the-group/03-DEVICE-PASS.md` in full. Copy its shape: a table per device, one row per observation, a verdict column, and a plain statement at the top of what was tested on what hardware and on what date.
    - `.planning/phases/02-practical-information/02-DEVICE-PASS.md` for the same reason.
  </read_first>
  <action>
    Create `.planning/phases/04-photos/04-DEVICE-PASS.md` in the shape phases 2 and 3 used: a header
    stating the device, the operating system version, the browser and the date, then one table per
    device with one row per observation and a verdict column, then a short closing section for
    anything found.

    The sheet must carry every line the UI spec's verification checklist names, as its own row:

    On real iOS Safari: the picker opens from an input carrying the hidden attribute; multi-select
    works; **a portrait photograph lands the right way up in the album**; an HEIC pick from the camera
    roll arrives as a JPEG and uploads.

    On real Android Chrome: the same four minus the HEIC line.

    On a throttled connection: progress stays visible and honest, and the bar never sits at full
    width waiting for a response.

    The sixth photograph is refused with the joke, on a real phone, and the copy nowhere reads as a
    fault.

    The album grid, the queue and the control at 320px width, in Danish.

    Every touch target measured against the touch geometry table, with the measured numbers written
    down rather than a tick.

    Reduced motion enabled at the operating system level: the progress bar still updates in steps and
    is neither hidden nor parked, the pressed feedback is instant, and the body swap is instant.

    VoiceOver and TalkBack: progress is announced politely, a failure is announced assertively, and an
    album tile announces whose photograph it opens.

    A 12 megapixel photograph does not look soft or aliased in the album at four columns, which is
    research assumption A1, and five sequential decodes do not reload the tab, which is A2.

    Fill it in by running the pass. A row that could not be tested is recorded as not tested with the
    reason, never as passed. If the orientation row fails, stop and record it as the phase's blocking
    finding: it is the one claim in the whole design contract that no probe could settle, and the
    remedy named in research is a five line change confined to one function. If the sharpness row
    fails, record it and name stepwise halving as the remedy, and do not build it speculatively.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-photos/04-DEVICE-PASS.md &amp;&amp; echo "sheet exists"</automated>
    <automated>for t in portrait HEIC multi-select throttl 320px "reduced motion" VoiceOver TalkBack refused; do grep -qi "$t" .planning/phases/04-photos/04-DEVICE-PASS.md || { echo "MISSING row: $t"; exit 1; }; done; echo "all required rows present"</automated>
    <automated>test "$(grep -ci 'to be filled\|TBD\|TODO' .planning/phases/04-photos/04-DEVICE-PASS.md)" = 0 &amp;&amp; echo "no unfilled placeholders"</automated>
    <automated>node -e "var s=require('fs').readFileSync('.planning/phases/04-photos/04-DEVICE-PASS.md','utf8');if(/[\u2013\u2014]/.test(s))throw new Error('en or em dash found');console.log('dash scan clean')"</automated>
    <human-check>
      The pass itself. Every row in the sheet is an observation made on the named hardware, on the
      named date, by a person holding the phone. The orientation row and the HEIC row cannot be
      satisfied any other way and must not be marked passed on the strength of an emulator.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-photos/04-DEVICE-PASS.md` exists and names the device, the operating system version, the browser and the date for each device tested
    - every required row is present, asserted by the loop above
    - every row carries a verdict of passed, failed, or not tested with a reason. No row is blank and no row says it will be filled in later
    - the touch target rows carry measured numbers, not ticks
    - the orientation row states plainly whether a portrait photograph landed the right way up, and if it did not, the finding is recorded as blocking and the remedy named
    - zero em dashes and zero en dashes in the file
    - the sheet is referenced from the plan summary, and any failed row is carried into the summary's outstanding list rather than only into the sheet
  </acceptance_criteria>
  <done>Every claim in this phase that only a phone can settle has been settled on a phone, written down with a verdict, and the one that could go wrong most visibly has its own line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository to the live database | `supabase/schema.sql` describes what the project should be; only a human at the dashboard makes it so. The gap between the two is invisible from the site. |
| publishable key to the public | Anyone reading the page source can call both services directly. The bucket record is the only thing between them and the storage tier. |
| owner's dashboard session to the data | The only path that can delete anything, on purpose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-27 | Repudiation | The file claiming a bucket limit the live project does not have | high | mitigate | Task 1 states explicitly that it does not claim application, and task 2 is a blocking human gate whose acceptance is a refused four megabyte upload followed by a failed public read of that object, not a status code. On this project a blocked read answers with an empty array and a blocked delete answers 204, so a status code is not evidence. |
| T-04-28 | Denial of service | Storage exhaustion by anyone holding the publishable key | high | mitigate | The byte-counted ceiling is the only control that holds against a crafted request, and this plan is what puts it on the live bucket. The count cap is per identity and resettable, which `REQUIREMENTS.md` already accepted. The free tier's one gigabyte and the owner's dashboard are the remaining recourse. |
| T-04-29 | Tampering | A future run "reconciling" the two size numbers into one | medium | mitigate | The config comment written in plan 04-01 and the §6 comment written here both state that the twelve megabyte client check and the three mebibyte bucket ceiling do two different jobs, one before the shrink and one after it. Both are in writing so neither reads as an accident. |
| T-04-30 | Information disclosure | The type allow-list being described as protection it is not | medium | mitigate | The §6 comment states that Supabase validates the declared content type rather than the bytes, so the list stops accidents and not a deliberate request. The acceptance criteria assert the comment says both things and describes neither as the other. |
| T-04-31 | Information disclosure | A read or delete rule added to a table while touching this file | high | mitigate | Gated: zero delete policies in the whole file, zero select policies on the photos table, zero invoker views, the trigger keeps owner rights, and §9's revoke is intact. §9's revoke is safe to keep only while §4 carries owner rights, and both are asserted together. |
| T-04-32 | Information disclosure | Test rows attributed to a fictional guest left in a public album | medium | mitigate | The owner removes them and the removal is proved through `public.album` returning an empty array, never through the blocked table. The album's path allowlist already keeps the research paths from rendering, so the exposure was bounded before the cleanup as well. |
| T-04-SC | Tampering | Dependency supply chain | low | accept | Nothing installed by this plan or this phase. No package manager exists in this repository. |
</threat_model>

<verification>
Run from the repository root:

1. All seven schema gates in task 1 print their success lines.
2. The blocking handoff in task 2 is completed and the output of all three probes is pasted into the plan summary verbatim.
3. Probe 1 shows the four megabyte upload refused and the public read of that object returning 400.
4. Probe 2 shows the plain-text upload refused.
5. Probe 3 prints exactly `[]`.
6. `04-DEVICE-PASS.md` exists, every required row is present, and no row is blank or deferred.
7. `git diff --stat` shows exactly two files touched: `supabase/schema.sql` and the new device pass sheet.
</verification>

<success_criteria>
- The live `party-photos` bucket refuses a four megabyte object and refuses a plainly non-image declared type, proved by attempting both and reading the resulting state.
- `public.album` returns an empty array, so no fictional guest is attributed a photograph in the shared album.
- The file no longer describes the five-photo refusal as unproven, and describes neither of the bucket's two limits as something it is not.
- A portrait photograph taken on an iPhone appears the right way up in the album, recorded on a real device with a date and a verdict.
- Every outstanding finding, if any, is in the plan summary and not only in the record sheet.
</success_criteria>

<output>
Create `.planning/phases/04-photos/04-05-SUMMARY.md` when done.
</output>
